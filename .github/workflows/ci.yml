name: CI

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]
  workflow_dispatch:
  schedule:
    - cron: '0 6 * * 1'   # weekly full-matrix drift net (Mondays 06:00 UTC)

# A superseded push cancels its own stale run — but only its own kind.
# Phase 115: the group used to be ci-<ref>, so the weekly scheduled
# full-matrix drift net shared a lane with ordinary pushes to main and a
# Monday-morning push silently cancelled it (and vice versa) with no
# failure signal — the one run that exists to catch what the per-push
# linux-only runs miss was the one most likely to be killed. Keying on
# event_name as well gives schedule, workflow_dispatch and pull_request
# their own lanes, while pushes still cancel superseded pushes.
concurrency:
  group: ci-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Phase 82 CI-diet: ubuntu runs the full suite on EVERY push (the fast
  # always-on referee); windows + macos run only when the push touches
  # portability-sensitive surfaces — the scripts/hooks/libs the scenarios
  # actually execute — plus on tags, manual dispatch, PRs, and the weekly
  # schedule. Zero scenario deletions, zero assertion weakening; the full
  # matrix stays one workflow_dispatch away.
  detect:
    name: Detect portability surfaces
    runs-on: ubuntu-latest
    outputs:
      os_list: ${{ steps.diff.outputs.os_list }}
    steps:
      - uses: actions/checkout@v4
        with:
          # Phase 107: full history so the ENTIRE push range is diffable.
          # fetch-depth 2 only reached HEAD^, so a multi-commit push whose
          # TIP was docs-only ran linux-only even when earlier commits
          # touched scripts/hooks/libs — which is exactly how Phase 106's
          # safety code got single-platform coverage. Cost: a few seconds
          # on one ubuntu job, against shipping portability-sensitive code
          # unverified on two platforms.
          fetch-depth: 0
      - id: diff
        shell: bash
        run: |
          full='["ubuntu-latest", "windows-latest", "macos-latest"]'
          linux='["ubuntu-latest"]'
          if [ "${{ github.event_name }}" != "push" ] || [ "${{ github.ref_type }}" = "tag" ]; then
            echo "os_list=$full" >> "$GITHUB_OUTPUT"
            echo "full matrix: event=${{ github.event_name }} ref_type=${{ github.ref_type }}"
            exit 0
          fi
          # Diff the whole push range, not just its tip. Fail OPEN (full
          # matrix) on branch creation (all-zero before), a missing before,
          # or a before this clone cannot resolve (force push).
          before='${{ github.event.before }}'
          case "$before" in
            ''|0000000000000000000000000000000000000000)
              echo "os_list=$full" >> "$GITHUB_OUTPUT"
              echo "full matrix: no before-sha (branch creation) — fail-open"
              exit 0 ;;
          esac
          if ! git rev-parse --verify "$before^{commit}" >/dev/null 2>&1; then
            echo "os_list=$full" >> "$GITHUB_OUTPUT"
            echo "full matrix: before-sha unreachable (force push?) — fail-open"
            exit 0
          fi
          changed=$(git diff --name-only "$before" HEAD)
          echo "$changed"
          if printf '%s\n' "$changed" | grep -qE '^(scripts/|\.claude/(scripts|hooks|lib)/|template/\.claude/(scripts|hooks|lib)/|\.github/(test-fixtures/|workflows/ci\.yml))'; then
            echo "os_list=$full" >> "$GITHUB_OUTPUT"
            echo "full matrix: portability surface touched"
          else
            echo "os_list=$linux" >> "$GITHUB_OUTPUT"
            echo "linux-only: no portability surface in this push"
          fi

  test:
    name: Test (${{ matrix.os }})
    needs: detect
    runs-on: ${{ matrix.os }}
    # Phase 115: without this the job inherits GitHub's 360-minute default,
    # so a wedged interactive prompt (update.sh reads stdin in several
    # scenarios) burns six hours of runner time before anything is
    # surfaced. Measured across the last eight runs: ubuntu ~70s, macOS
    # ~102s, windows 678-881s. 45 is 3x the worst observed, which leaves
    # room for the suite to keep gaining a scenario per phase without
    # re-tuning, and still surfaces a hang 8x sooner than the default.
    # Deliberately not 20-30: that sits close enough to the 30% windows
    # spread to risk spurious red, and a flaky suite is the failure mode
    # this phase exists to prevent.
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        os: ${{ fromJSON(needs.detect.outputs.os_list) }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Show environment
        shell: bash
        run: |
          uname -a
          python --version
          command -v sha256sum >/dev/null 2>&1 && echo "sha256sum: yes" || echo "sha256sum: no"
          command -v shasum     >/dev/null 2>&1 && echo "shasum: yes"     || echo "shasum: no"
          command -v jq         >/dev/null 2>&1 && echo "jq: yes ($(jq --version))" || echo "jq: no"
          bash --version | head -1

      - name: install.sh --help
        shell: bash
        run: bash scripts/install.sh --help >/dev/null

      - name: update.sh --help
        shell: bash
        run: bash scripts/update.sh --help >/dev/null

      - name: Scenario - fresh-install
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh fresh-install

      - name: Scenario - raw-baseline-install
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh raw-baseline-install

      - name: Scenario - install-uuid-fresh
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh install-uuid-fresh

      - name: Scenario - install-uuid-backfill
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh install-uuid-backfill

      - name: Scenario - share-enable-fresh-remote
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh share-enable-fresh-remote

      - name: Scenario - share-status-disabled-default
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh share-status-disabled-default

      - name: Scenario - share-capture-redact
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh share-capture-redact

      - name: Scenario - share-produce-enabled
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh share-produce-enabled

      - name: Scenario - share-produce-idempotent
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh share-produce-idempotent

      - name: Scenario - share-produce-disabled
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh share-produce-disabled

      - name: Scenario - share-push-empty-remote
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh share-push-empty-remote

      - name: Scenario - share-push-race
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh share-push-race

      - name: Scenario - share-push-enabled
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh share-push-enabled

      - name: Scenario - share-push-failsoft
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh share-push-failsoft

      - name: Scenario - share-push-disabled
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh share-push-disabled

      - name: Scenario - share-push-manual
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh share-push-manual

      - name: Scenario - share-preview-enabled
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh share-preview-enabled

      - name: Scenario - share-preview-disabled
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh share-preview-disabled

      - name: Scenario - share-purge-remote
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh share-purge-remote

      - name: Scenario - share-purge-nothing
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh share-purge-nothing

      - name: Scenario - share-purge-confirm-eof
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh share-purge-confirm-eof

      - name: Scenario - share-disable-plain
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh share-disable-plain

      - name: Scenario - graduation-review-report
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh graduation-review-report

      - name: Scenario - graduation-review-disabled
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh graduation-review-disabled

      - name: Scenario - fresh-refuse
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh fresh-refuse

      - name: Scenario - merge-add
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh merge-add

      - name: Scenario - local-mod-detect
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh local-mod-detect

      - name: Scenario - local-mod-preserve
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh local-mod-preserve

      - name: Scenario - backfill-migrate
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh backfill-migrate

      - name: Scenario - raw-baseline-migrate
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh raw-baseline-migrate

      - name: Scenario - check-remote-cached
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh check-remote-cached

      - name: Scenario - hook-fail-closed-bash-safety
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh hook-fail-closed-bash-safety

      - name: Scenario - cruft-check-fixture
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh cruft-check-fixture

      - name: Scenario - replace-with-yes-piped
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh replace-with-yes-piped

      - name: Scenario - hook-fp-exemption-git-commit-message
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh hook-fp-exemption-git-commit-message

      - name: Scenario - hook-destructive-canonicalization
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh hook-destructive-canonicalization

      - name: Scenario - update-integrity
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh update-integrity

      - name: Scenario - interpreter-probe-class
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh interpreter-probe-class

      - name: Scenario - update-working-tree-safety
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh update-working-tree-safety

      - name: Scenario - path-anchoring
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh path-anchoring

      - name: Scenario - git-context-safety
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh git-context-safety

      - name: Scenario - redaction-windows-identity
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh redaction-windows-identity

      - name: Scenario - write-atomicity
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh write-atomicity

      - name: Scenario - share-disable-tear
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh share-disable-tear

      - name: Scenario - watchdog-transcript-resolution
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh watchdog-transcript-resolution

      - name: Scenario - watchdog-dedup-reobserve
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh watchdog-dedup-reobserve

      - name: Scenario - match-rebaseline
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh match-rebaseline

      - name: Scenario - rebase-only-persist
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh rebase-only-persist

      - name: Scenario - check-remote-identity
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh check-remote-identity

      - name: Scenario - install-rerun-refuse
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh install-rerun-refuse

      - name: Scenario - telemetry-generator-fixture
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh telemetry-generator-fixture

      - name: Scenario - telemetry-python3-only
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh telemetry-python3-only

      - name: Scenario - cost-line-sitting-delta
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh cost-line-sitting-delta

      - name: Scenario - goals-surface
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh goals-surface

      - name: Scenario - audit-cadence-nudge
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh audit-cadence-nudge

      - name: Scenario - plugin-discovery-manifest
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh plugin-discovery-manifest

      - name: Scenario - watchdog-agent-duration
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh watchdog-agent-duration

      - name: Scenario - plugin-context-matcher
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh plugin-context-matcher

      - name: Scenario - fold-status-preserve
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh fold-status-preserve

      - name: Scenario - receipt-render
        shell: bash
        run: bash .github/test-fixtures/scenarios.sh receipt-render
