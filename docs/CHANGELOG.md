# Changelog

All notable changes to claude-skeleton are documented here. Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/); versioning follows [SemVer](https://semver.org/).

## [Unreleased]

*(v1.1.5 cut on 2026-07-15. Next queued: the TV/EoG propagation pass runs from those repos; v1.2.0's remaining components land as their signals mature.)*

- Micro-pass: watchdog Agent-duration signal (observation 6708b966 disposition) — task-watchdog's long-running-duration gate extends Bash-only → Bash + Agent. Agent awaits measure via `toolUseResult.totalDurationMs` with a tool_use→tool_result timestamp-delta fallback (covers cancelled awaits); separate **evidence-derived** threshold `AGENT_DURATION_THRESHOLD_MS=3600000` — across 25+ historical dispatches in this project's transcripts the legitimate synchronous-await ceiling was 46.5 min (next 45.4, then ≤23.6) vs wedge-class instances at 239–325 min, so 60 min clears the observed max with margin and catches every wedge; async launch-acks (~1s) never trip it. Agent-namespaced signatures (`agent:` + normalized description — no bash-signature collisions), tool-aware summaries/notes, `target_resource: tool:Agent`; Phase 67 dedup/replay and resolve semantics untouched; no new signal classes. Guard `watchdog-agent-duration` (2h await → exactly one agent-dispatch observation; 10-min await silent; Bash 400s branch unregressed), red pre-ship / green post, ci.yml same-commit; both existing watchdog scenarios re-verified green. Doc truth in-pass: task-watchdog.md description/signals-table/target_resource + roster row 11 (both mirrors). Observation 6708b966 resolved with the fix (gitignored-local runtime). Single commit.

- Micro-fix: long-running dispatch hygiene — the cycle-two optimizer dispatch wedged 7+ hours Running and was manually cancelled; the subagent transcript (`agent-ad4df7ef…jsonl`, 62 entries) gives the evidence-grounded diagnosis: **work complete 8 minutes in** (both drafts staged 14:55–14:57Z, counter reset written), a 3h47m harness stall before that final Write's tool_result returned, the agent's complete return summary emitted at 18:48:22Z — and nothing after: the await/return leg died post-completion (0-byte task output, parent Running until cancel). Second instance of the hung-await class (the CI-watch false-alarms were the first, fixed by the conclusion-field check); same lesson codified as a manager paragraph in § Per-project manager-optimizer + a cross-ref in § Scheduled dispatch-class audits (both mirrors): dispatches stage to disk BEFORE returning by design — on a hang past expected scale, check the staged surfaces, cancel, recover from the staged files and ledger; **never re-dispatch first**. Companion finding flagged, not fixed: task-watchdog's duration signal is Bash-only (`task-watchdog.sh:289`), so the 7h Agent dispatch was invisible to it — recorded as observation `6708b966…` (source `manual`, registered v1.1.0 enum value; no producer changes this pass) for normal triage. Docs + diagnosis + one observation; single commit.

- Phase 76 v1.5 foundations — recommendation schema + plugin-discovery, the first v1.5 components, grounded in the read-only recon of `~/.claude/plugins/` (all design facts evidence-locked before build). **`recommendation.schema.md`** (template + mirror, in a new `.claude/recommendations/` dir): the per-install manifest contract — context frontmatter is mechanical file-evidence (stack markers, `.claude` inventory, observation histogram) plus the **user-owned `discipline_preferences` block that discovery never touches after creation**; body entries carry `status` (candidate | recommended | not_recommended | installed), `source_class` (repo_hosted | external_sha | unindexed), **`version` OPTIONAL by design** (155 of 168 official plugins publish none — absence is normal, never an error), and **`reason` REQUIRED on recommended AND not_recommended — a rejection without a reason fails the schema**; external-sha entries carry source url + pinned sha + the explicit `source_not_inspected_offline` marker. **`plugin-discovery.sh` + `plugin-discovery-agent`** (template + mirrors; the cruft-checker "script is the contract" shape — CI can't dispatch a model): closed inputs (marketplace clones, known/installed registries, project-context surfaces) → the manifest as `status: draft` — installed plugins enter as `installed` never candidate, everything else `candidate` with its marketplace.json entry as evidence; **zero verdicts (Phase 77 matcher territory), no scoring, no network, no install/enable ever — `/plugin` stays the only install path**; empty/absent marketplaces → honest stub. Manifest gitignored (runtime output; the schema beside it is the tracked contract). Registered as the audits registry's third entry (`plugin_discovery`, seed cadence 30, BOTH gate-configs — ships in template) — resolving ROADMAP's "SessionStart hook for plugin-aware suggestions" line to the shipped Phase 74 registry mechanism (sessions cadence, not a new hook). Guard `plugin-discovery-manifest` (5 legs incl. discipline-block-survives-refresh), red pre-ship / green post, ci.yml same-commit; fixtures 75→78 at the three sites. In-phase roster updates (T1 eighteen helpers / twelve scripts, roster row 18, ROUTING + ARCHITECTURE, manager H2 with the one-breath code-quality-auditor boundary); marker refreshed via the guarded self-run (78/78). **First live dispatch (verification): 179 plugins indexed across 3 marketplaces (168 official), 172 candidates / 7 installed, 129 external-sha markers, zero verdict strings, true context frontmatter, zero writes outside the manifest.** Three commits. No VERSION bump. **Propagation:** deferred — Phase 76 rides ONE bundle with Phase 77 to TV/EoG.

- Phase 75 roadmap-auditor — the second locked audit surface ships: skeleton-level claim integrity, **dogfood-only** (lives in `.claude/agents/05_meta/` with no template mirror — the two-surfaces principle; targets see one manager sentence, not the audit). Five closed lanes, each requiring file:line evidence on BOTH sides: SHIPPED-VS-CLAIMED, GATE-INTEGRITY, CROSS-DOC CONTRADICTION, CONTRACT-DRIFT, PRINCIPLE-VIOLATION (grep-able patterns from the locked lists; historical records are evidence, never findings). Output = observations per the shared schema (source registered both mirrors per the Phase 65 procedure, `other` + lane-prefixed notes, local-only, full-resolve semantics); clean-bill per lane is an expected honest output; the auditor never edits what it audits. Registered as the audits registry's second entry (dogfood gate-config, cadence 25 — deep audit, not a linter; template gate-config untouched, its "not yet built" staying true for installs); the Phase 74 "anticipated second" manager sentence resolves (both mirrors — the one template touch). Roster surfaces exempt per the cruft-checker dogfood-only precedent. **First live dispatch (verification): 4 dual-evidenced findings, 3 clean lanes** — ROADMAP:5 still says "onboarding next" (tier complete at [1.1.5]); ROADMAP:80/:81 mark infrastructure-auditor and roadmap-auditor as future (shipped 74/75); cruft-checker.md:11 describes the coordinator as a future template agent — with GATE-INTEGRITY, CONTRACT-DRIFT, and PRINCIPLE-VIOLATION clean (planted-fixture red / real-docs green proved the lane-5 patterns). Findings route to normal triage; the dispatch was recorded via the Phase 74 manager-flow rule's first live use; zero tracked-tree writes during dispatch. Three commits. No VERSION bump. **Propagation:** only the manager sentence + schema registration ride the TV/EoG pass; the auditor itself never propagates.

- Phase 74 infrastructure-auditor — v1.2.0's scheduled-audit coordinator, deliberately **not an agent** (an agent whose only job is printing "audit due" would fail the fit-analyzer's own MISFIT lane); built entirely from existing surfaces. `gate-config.json` gains the extensible `audits` registry (first registrant `artifact_fit_analyzer`, `sessions_between_dispatches: 18`; `roadmap-auditor` named as the anticipated dogfood-only second, not built); per-audit session counters live in gitignored `telemetry/audit-state.json`, incremented every SessionEnd by a sibling stanza to the Phase 53 optimizer counter (same hook, same pattern — no second mechanism); the SessionStart rules chain gains job 6 (inline bash+jq — the hook already requires jq; zero new files, template count stays 75): enabled audits past cadence surface as ONE `[infrastructure-audit]` due line, 24h anti-repeat marker, absent state counts as 0 so fresh installs never open with an audit nag. **Nothing auto-dispatches** — the flow is line → human go → manager dispatches the named auditor and resets its audit-state entry (the fit-analyzer's never-list forbids agent-side telemetry writes, so recording is the dispatcher's job; a bypassing dispatch just lets the line resurface — fail-annoying, not fail-wrong). Cadence units are sessions, never calendar (locked principle); the wall-clock marker is noise-gating only. New CLAUDE_MANAGER H2 (both mirrors) incl. the three-ambient-lines boundary (optimizer nudge = proposals await review; goals = approved spec due; audit = cadence passed); the fit-analyzer section's "cadence deferred to infrastructure-auditor" resolves to the shipped coordinator; hooks/README six jobs. Guard `audit-cadence-nudge` (counter-creation, due-line, under-cadence, cooldown legs), red pre-ship / green post, ci.yml same-commit; adjacent chain scenarios re-verified. Dogfood live: audit line silent, counter at 1. Marker refreshed via the guarded self-run. Three commits. No VERSION bump. **Propagation:** TV/EoG pick up 74 with the pending pass.

## [1.1.5] - 2026-07-15 — The onboarding tier ships

**First cut since v1.1.4 — three arcs in one release:**
- **v1.2.0 per-project components, ahead of schedule:** `manager-optimizer` (Phase 53 — including the first full governance cycle run on real dogfood state), `artifact-fit-analyzer` (56), `/goals` + scheduled goals (68), and the gate/friction config surface (48).
- **Hardening + truth (Phases 57–67):** update-pipeline integrity, telemetry-chain hardening with its first CI coverage, the docs truth sweep and its in-phase roster directive, per-sitting cost semantics, watchdog producer fixes — plus four standing commit/plan disciplines codified from the project's own misses.
- **Onboarding (Phases 69–73, complete):** README reframe, GETTING-STARTED, the plugins story, the post-install orientation + placeholder fix, and the first-run welcome.

- Phase 73 first-run SessionStart welcome (v1.1.5-E) — **the onboarding tier is complete (A–E all shipped; the tier-completion release call is a separate decision).** One-time welcome on the first session of a freshly-installed project: install.sh drops a gitignored `.claude/.first-run` flag (timestamp content, non-dry-run, after the marker write); `sessionstart-rules.sh` job 5 (pure bash, inline — no new hook or script file) consumes the flag FIRST (deletion is the fire-once absolute) and emits a ~7-line orientation block at the top of the combined context (welcome → rules → drift → watchdog → goals; the combined-output assembly simplified to one append_block chain). Flag absent → one `[ -f ]` test, zero output. Existing installs provably never see it: update.sh never creates the flag and install.sh refuses installed targets (Phase 62) — dogfood live-run verified welcome-free. The flag is runtime state outside the template inventory, so the Phase-72 fresh-install UNCHANGED invariant is untouched (the scenario's dry-run-clean assert still proves it). Guards: `scenario_fresh_install` extended — flag present post-install, first hook run prints welcome + Durable-rules default and consumes the flag, second run silent, a real update.sh run doesn't recreate it, third run still silent; red pre-fix / green post; ci.yml untouched (existing step). hooks/README (both mirrors) enumerates job 5 (the one surface that enumerates — CLAUDE_MANAGER doesn't, verified per the Phase 64 rule). GETTING-STARTED's transcript re-transcribed from this phase's sandbox (welcome + Durable-rules default; the `{{COMPACT_PROMPT}}` line and its "looks like a bug" paragraph retire post-72). Mirrored-hook baselines self-heal via match-rebaseline on the next dogfood update. Three commits. No VERSION bump. **Propagation:** rides the pending TV/EoG pass (harmless there — no flag, silent no-op; byte-parity preserved).

- Phase 72 install.sh post-install message + {{COMPACT_PROMPT}} fix (v1.1.5-D) — the post-install closing is now an orientation block (real per-class counts computed from the installed tree; the nothing-runs-without-approval reassurance; GETTING-STARTED by path + URL; the `/goals` first move mirroring the guided dispatch; tuner + mechanics pointers), and `resolve_default_compact_prompt()` substitutes a generic project-agnostic Durable-rules default into the installed `settings.json` — no literal placeholder ever greets a fresh install again (the Phase 70 sandbox finding, closed). The template keeps its placeholder as the tuner's detection surface. **Recon correction recorded:** the brief's "consistent with existing {{PROJECT_NAME}} handling" had no referent — install.sh had zero substitution machinery; all other placeholders remain tuner-owned by design (three more live inside `.claude/` — deploy.sh's DEPLOY_COMMAND and two doc slots — none greet). **Edit-time correction recorded:** the approved plan's marker-hash re-record clause was inverted at implementation — re-recording would classify the resolved file TEMPLATE_UPDATED and offer to regress it to the raw placeholder; not re-recording yields LOCALLY_MODIFIED, the fleet-standard prompt-protected state, and the guard asserts exactly that. `scenario_fresh_install` extended (message + no-placeholder + Durable-rules default + first-dry-run classification), red pre-fix / green post. Two commits. No VERSION bump. Skeleton-root tooling — no mirror, but fresh installs from the updated checkout get the new behavior immediately. **[Superseded same-phase by the C hotfix below — the install-time substitution mechanism did not survive CI.]**
  - **Phase 72 C hotfix (CI red on all three OSes):** the install-time substitution's LOCALLY_MODIFIED-by-default choice knocked `install-uuid-backfill` off the early-exit path (backfill worked; its message didn't print) and would have poisoned every clean-fresh-install fixture premise (match-rebaseline counts, rebase-only-persist's empty buckets). Correct shape — the brief's own alternative branch: the generic Durable-rules default ships AS `settings.json.template`'s compactPrompt value (no placeholder). Installed == template == baseline → a fresh install's settings.json is genuinely UNCHANGED; no greeting placeholder, no first-update prompt, no install-time machinery (`resolve_default_compact_prompt()` reverted unreleased; the summary() message rewrite stays). Tuner: seven `{{...}}` slots + refining the shipped default (its last line says so); INSTALLATION's tuner bullet corrected (retiring the never-fixed "nine"). Eight affected scenarios verified green (six working-tree + two via committed overlay). **Propagation note:** TV/EoG's settings.json copies are LOCALLY_MODIFIED divergent files — protected behind the [K]eep-default prompt as always; their update behavior is unchanged by the template's new default.

- Phase 71 PLUGINS-GETTING-STARTED.md (v1.1.5-C) — the opt-in plugin story. Sections per the outline: plugins are opt-in (skeleton installs none; the auditor watches the cache on its cooldown); the vetted six one breath each with the honest externality note (descriptions are vetting-time rationale, not live guarantees — external code updates on its own schedule); the eyes-open install pattern with claude-mem as the worked example (the four HALT-ON-CONCERN findings from PLUGIN-INSTALLS-v1.1.4.md § Trust-tier-2 vetting, incl. the punchline that two live outside the audit triad's visibility by design — accepted in writing, which IS the pattern); trust tiers in one paragraph stating plainly that two operationally-defined tiers are the whole codification today (`--candidate` mode = planned v1.5-B); the auditor's three heuristics in plain terms with findings → observations → triage; pointers incl. the one-line note that recommendations-with-reasons are the future tier this manual-era discipline precedes. GETTING-STARTED's plugin-auditor bullet gains the cross-link. Repo-side doc, no template mirror, update-neutral. Single docs commit; no VERSION bump.

- Phase 70 GETTING-STARTED.md (v1.1.5-B) — the first-15-minutes doc, written from mechanical ground truth: a scratch sandbox fresh install had its SessionStart chain fired and transcribed verbatim, and §"Your first session start" is that transcript — the literal `{{COMPACT_PROMPT}}` (un-tuned installs; looks like a bug, isn't — the doc says so), the drift cache-empty block, the first-session watchdog cosmetic (expected, disappears next session), the plugin auditor's silence, no cost line until a session completes (the appears-later shape transcribed from a seeded-rollup variant, fresh-lineage form with no lineage segment). Sections per the outline: the arrived `.claude/` tree with re-derived counts (17/6/11/10/7) + the marker's purpose + the reassurance-with-teeth; the transcript walk; first dispatch = `/goals` on a real small goal (whole gate pattern in one pass; artifact-fit-analyzer as the one-line alternative); the loop from the user's seat; tuning surfaces (gate-config per-sitting thresholds, CLAUDE_MANAGER as theirs to diverge, update.sh classification); pointers. Repo-side doc (`docs/GETTING-STARTED.md`), no template mirror, update-neutral. README's install section gains the deferred Phase 69 pointer flip. Single docs commit; no VERSION bump.

- Phase 69 README onboarding refresh (v1.1.5-A) — the outward-facing era opens. The README was pitch-shaped and predated the live loop; rewritten to the onboarding outline in peer register: opener (per-project governance layer, one immune-system clause), "What it actually does" (six one-sentence claims, each traceable to a shipped file — producers → captures → approval → shipped artifacts incl. the two real shipped captures as the proof point, cooldown session-start checks, the Phase 66 per-sitting cost line, the draft-only optimizer, the /goals pipeline), install (install.sh fresh / update.sh existing with the Phase 62 refusal noted; INSTALLATION pointer only — GETTING-STARTED arrives in Phase B), "What it is not" (four lines condensed from STORY's fence), honest current state (v1.1.4 + [Unreleased], TV + EoG + dogfood, tier line), pointers (outline's four + CLAUDE_MANAGER kept as a stated addition). Phase 64 de-rot pattern preserved — zero commit-pinned claims. Single docs commit; no VERSION bump; repo-facing only (no template mirror, no propagation).

- Phase 68 /goals expanded + scheduled goals — the long-open v1.2.0 pure-design component ships. Honest baseline: **/goals did not previously exist** (the CLAUDE_MANAGER references were explicit future-hook callouts), so "expanded" built it fresh to the expanded design and there was no data to migrate. **The pipeline** (`.claude/commands/goals.md`, both mirrors): RESEARCH — repo-local reads only, Tier 1, no web, no mutations; CLARIFY — exactly ONE batched round of numbered questions covering only research-unanswerables (zero questions is a valid outcome; terse-reply format; no second round uninvited); SPEC — structured doc at `.claude/specs/<slug>.md` per the new `goal-spec.schema.md` (5 frontmatter fields incl. optional `schedule` with stateless due semantics — ISO date / daily / weekly=Mondays / monthly=1st; 5 body sections), always `status: draft`. Nothing builds from any spec without the user's draft→approved flip — the command states the load-bearing line itself. **Scheduled goals**: `goals-surface.sh` (execution-validated probe, 24h cooldown marker written only on surface, approved-only, ≤1 ambient line) wired as `sessionstart-rules.sh`'s fourth job — SessionStart hooks only, never cron/`claude -p`. **Guards**: `goals-surface` scenario (approved+due → exactly one line; overdue-but-draft → silent; `--hook` cooldown honored), red pre-ship / green post, ci.yml same-commit; fixtures 71→74→75 per commit (three new counted files in A, one in B). In-phase roster updates per the Phase 64 directive (T1 eleven scripts, ROUTING +2 rows, hooks/README 4-job list; ARCHITECTURE exempts — no command/script surface there). The two future-hook callouts resolved; integration-checker remains the only pending stub. **Live dry-run**: the pipeline ran on the real propagation goal — research answered all blocking unknowns so the clarify round asked zero questions (the rule working as designed), and the first live spec landed at `.claude/specs/propagate-skeleton-tv-eog.md` (draft, `schedule: 2026-07-15`) — approving it makes the propagation surface at session start when due. Three commits. No VERSION bump. **Propagation:** TV/EoG queue is now Phases 48–68.

- Phase 67 task-watchdog producer fixes — the two defects the first triage circuit surfaced, root-caused against the resolved observation corpus before fixing. **(1) Evidence ×2 duplication — diagnosis corrects the brief's hypothesis** (marker miss / overlapping windows): the corpus shape (entire event sequence repeated once in order; 26 = 13×2 under the cap's `(prior+new)[-20:]` arithmetic) plus a functioning marker prove the real mechanism is the Phase 66 lineage phenomenon — resumed sessions replay the full prior history under a NEW sessionId, the marker check passes legitimately, and `write_observation`'s merge branch appended blindly. The merge now dedups by event identity; only new evidence counts occurrences, `last_seen` advances only on new signal, and a pure replay is a true no-op (no rewrite — so a triaged observation's `resolved_at` is not regression-reset for zero new evidence). Forward-only; resolved corpus verified byte-identical. **(2) Coarse signature**: "exit code n" bucketed four unrelated one-off commands into a false ×8 recurrence — Bash failure signatures now append `|exit:<code>|cmd:<head>`, splitting heterogeneous one-offs into sub-threshold buckets while genuine recurrences still accumulate; non-Bash signatures and all thresholds unchanged; new pattern_ids forward-only. Guard: `watchdog-dedup-reobserve` (legitimate ×3 recurrence emits; replayed lineage stays occ=3 with unique evidence; three heterogeneous exit-1 one-offs emit nothing), red pre-fix (occ doubled to 6) / green post, ci.yml wired same-commit. Mirror byte-identical. Two commits. No VERSION bump. **Propagation:** rides the combined TV/EoG pass with Phases 48–66.

- Phase 66 cost-line semantics — the SessionStart cost headline was the newest lineage's CUMULATIVE total labeled "last session" and compared to `warn_usd_per_session` (demonstrated on two installs: skeleton ~$479 spanning 11 days; EoG $52→$64 on a lineage resumed since June 10) — a false premise generator, and any long-lived lineage permanently tripped the warn once cumulative spend crossed the fixed value. Now (`sessionstart-cost-summary.sh` stanza 1, both mirrors): headline = **per-sitting delta** (latest rollup minus the previous checkpoint in the same lineage; single-rollup lineages: delta = total), token detail figures as the same per-sitting deltas, lineage cumulative kept as context with its start date (only when a prior checkpoint exists), and the threshold compares the **sitting** delta (marker `sitting>N`). Phase 53's 7d per-lineage window-delta logic untouched; fail-soft paths unchanged; no threshold values changed. `gate-config.json` `_meta.docs.cost` (both copies) documents the per-sitting semantics. Guard: `cost-line-sitting-delta` scenario (resumed-lineage delta + no false trip; converse heavy-single-sitting still trips), red pre-fix / green post, ci.yml wired same-commit. Dogfood live-fire: "last sitting ~$58.23 | lineage ~$945.16 since 2026-07-07" where the old line read the cumulative as "last session". **Semantics-shift flag (honest):** `warn_usd_per_session` values tuned against cumulative figures — EoG's fresh 200, TV's 3900 — compare a *different quantity* after this propagates and should be re-read against sitting-scale spend. This also closes the optimizer P1 rejection loop: P1 (raise warn to 200) was rejected with "fix the measurement first" — this is that fix, and the raise-to-200 rationale dissolves under sitting semantics. Two commits. No VERSION bump. **Propagation:** rides the pending TV/EoG pass.

- Phase 65 governance-schema cleanup + SESSION_LOG disposition — the review's last confirmed-medium cluster. **Producer fix** (`generate-session-telemetry.sh`, both mirrors): observations now schema-conformant on all four violated counts — filename is `<pattern_id>.json` (the schema requires 64-hex pattern_id as the filename, so the human-readable `token-telemetry-<session_id>` prefix moved into `target_resource`'s keeping), evidence entries carry a mechanical ≤120-char `summary`, `resolved_at` emits `null`, and no-data stubs report `confidence: low` instead of `high` (the related low). **Schema registration** (`session-observer.schema.md`, both mirrors — the documented enum-addition procedure, nothing else moved): `source` enum gains `session-end-telemetry`; `pattern_type` enum gains `token_telemetry` (referenced at the target_resource rule but never registered — same gap); the resolution-lifecycle asymmetry list gains the producer's point-in-time/session-bounded line (emits null, never resolves; intentional, task-watchdog rationale). **Guard**: `telemetry-generator-fixture` extended (already CI-wired) with the four conformance asserts plus an honest-stub leg; red pre-fix, green post. **Legacy migration** (local — observation files are gitignored per-install runtime): all **20** legacy files (review counted 19) migrated mechanically — renamed to `<pattern_id>.json`, `resolved_at` → null, summaries derived from each file's own totals, the **4** no-data stubs (review counted 3) set to `confidence: low`; validation sweep 0 nonconforming, 0 leftovers, nothing deleted. **SESSION_LOG retire-as-historical** (decision per brief): install-neutral header in `template/docs/SESSION_LOG.md.template` (writer retired Phase 58, nothing writes automatically, no live consumer); dogfood copy (gitignored) received the historical-record header locally — current operating record is this CHANGELOG. Live references verified clean (guarded backup + convention mentions only). Three commits. No VERSION bump. **Propagation:** rides the pending TV/EoG pass with Phases 48–64.

- Micro docs fix (post-64) — Pinball retired as a development target (user decision, 2026-07-12): the five live present-tense active-install claims corrected (README production list, ROADMAP target-projects line + Phase 35 evaluation window incl. its now-three-install close condition + the additive-sample note, STORY production list); rhetorical "pinball governs pinball" archetypes and historical records untouched; CLAUDE_MANAGER's "pre-pinball queue" nomenclature flagged, not fixed (mirrored surface, out of scope). Also repaired the mangled resolved-`{{DEPLOY_COMMAND}}` prose in dogfood ROUTING's two deploy rows (accepted resolved-placeholder divergence from the template sentence — pure substitution is what produced the nonsense). Single docs commit; no VERSION bump.

- Phase 64 docs truth sweep — the whole-system review found the doc layer ~8 phases behind the code; this phase fixed the cause and the state. **The cause:** new standing H3 `### Roster and doc surfaces update in the same phase` in `CLAUDE_MANAGER.md`'s commit-discipline cluster (both mirrors): any phase adding/retiring/renaming an artifact updates the roster, `ROUTING.md`, and `ARCHITECTURE.md` in-phase or records an explicit exemption; counts are re-derived mechanically (`ls | wc`), never incremented from memory. **The state** (every count derived against the actual dirs — 17 template agents / 10 scripts / 7 hook scripts across 4 events / 9 commands / 6 skills): CLAUDE_MANAGER roster 11→17 rows + T1 line corrected; manager-optimizer "(future)"-vs-"shipped v1" contradiction resolved; workflow-suggester rows read `.claude/observations/` not SESSION_LOG (roster + ROUTING); the "≥4 weeks stable" calendar qualifier replaced with ROADMAP's signal wording (locked principle — Phase 45's sweep missed this file); ROUTING.md + template gained the 5 share commands, 7 unrouted agents, 5 hook registrations (all 17 agents now route, verified mechanically); ARCHITECTURE un-frozen from "0.9.0" with all 13 meta agents; ROADMAP marks manager-optimizer (53) + artifact-fit-analyzer (56) + Phase 48 shipped and drops the never-existed `NEW_IN_TEMPLATE` class for update.sh's real vocabulary; README's "Latest:" de-rotted to a CHANGELOG pointer naming no commit; INSTALLATION's re-run-install advice replaced with the Phase 62 refusal + escape hatch + honest top-level-refresh limitation; hooks/README (mirrors) documents four events / all eight registrations incl. the PreToolUse safety pair; captures/README (mirrors) documents the 4-status lifecycle incl. `shipped`+`shipped_to` and corrects script-builder to shipped; `template/.gitignore.template` (the Phase 49 known-open) gains the runtime excludes the template's own hooks write, drops retired `.session-ended`, and replaces the untrue "merged into the target's .gitignore" claim with install.sh's real copy-only-if-absent behavior; update.sh `--help`'s two false `--auto-apply` claims corrected (help-string only, zero logic). **Recorded exemptions:** STATUS.md + PLUGINS.md are deliberately dogfood-absent (Dogfood mirror invariants — nothing mechanical reads them); dogfood-only artifacts stay unrouted in ROUTING per the cruft-checker precedent. **Flagged, not fixed:** the mangled resolved-DEPLOY_COMMAND prose in dogfood ROUTING's deploy rows. Three commits. No VERSION bump. **Propagation:** commit A's mirrored surfaces ride the queued TV / EoG / Pinball `update.sh` pass.

- Phase 63 telemetry-lib hardening — `generate-session-telemetry.sh` was the un-fixed twin of the Phase 57 task-watchdog bug (the silent-inert class), verified dead on python3-only systems and the Windows Store alias stub. Ported the Phase 57 donor pattern (both mirrors): presence-only `command -v python` gate → **execution-validated** python||python3 `PYBIN` probe; `encode_cwd` upgraded from the pre-57 narrow `[:\\/]` class to CC's real encoding (Git-Bash drive normalization, then every non-alphanumeric → `-`); GNU-only `find -printf` (silently empty on macOS) → portable `ls -1t`, returning **only on a hit** so the cwd-match fallback is actually reachable; the fallback's first-line `cwd` check (first lines never carry it — Phase 57 diagnosis) → bounded 40-line scan with normalized path comparison and per-dir/per-file OSError isolation; fail-soft-not-fail-silent — one stderr notice when transcripts exist but resolution missed, then the documented stub path. **Chain audit (same classes, whole Phase 46/48/53 chain):** `sessionend-cost-proposals.sh` + `sessionstart-cost-summary.sh` had python3 fallback but presence-only probes (the Store stub passes `command -v`, then every `"$PYBIN"` block dies under `2>/dev/null`) — both now execution-validated; `sessionend-observe.sh` swallowed the lib's stderr, killing the one-notice-line contract on arrival — unswallowed (`|| true` fail-soft retained). Behavior identical where `python` already resolves — verified by key-set diff of a fresh generation against a real transcript (rollup frontmatter + observation keys identical). **First CI coverage for the chain** (it shipped with none): `telemetry-generator-fixture` (synthetic transcript under a wrongly-encoded project dir — resolution must go through the fallback; asserts events lines, rollup totals against known sums, observation schema fields) and `telemetry-python3-only` (failing `python` stub + real `python3` shim; the pre-63 gate reproducibly wrote **zero artifacts**, the fixed probe falls back and writes all three), both red pre-fix / green post-fix, wired into `ci.yml` in the same commit as the scenarios (Phase 60 rule). Historical corrections added to the Phase 48/53 entries below (their "fixture-verified" claims were ahead of scenarios.sh reality). Three-commit cadence. No VERSION bump. **Propagation queued:** TV / EoG / Pinball pick up via `bash <skeleton>/scripts/update.sh`.

- Phase 62 update-pipeline integrity — three verified bugs from the whole-system review, each reproduced end-to-end pre-fix (red) and guarded green after. **(1) Rebase-only marker persistence** (`scripts/update.sh`): the "everything up to date" early exit gated its marker write on `BACKFILL_MODE || MIGRATED || IDENTITY_BACKFILLED` only; Phase 59's `RAW_BASELINE_REBASED` / `RAW_BASELINE_BACKFILLED` flags reached the main write guard but never this one — and since rebased/backfilled files classify `UNCHANGED`, a rebase-only or backfill-only run takes exactly this path, so the Phase 59 self-heal was computed, printed, and **discarded in its primary scenario** (template file updated + dogfood-mirrored in one commit, nothing else pending). Both flags now reach the early-exit guard, with the same summary lines; dry-run still writes nothing. Guard: `rebase-only-persist` (baseline persisted through the early exit; second dry-run reports the class drained). **(2) `--check-remote` identity destruction**: `check_remote` passed 10 of 13 args to `write_marker_json`, silently deleting `install_uuid`/`install_label`/`install_created`; the next full update then minted a NEW uuid — violating the never-regenerated contract and orphaning shared-memory history keyed by the old one. Full 13-arg set now passed; call-site audit confirmed the other two callers (update.sh main path, install.sh) were already complete. Guard: `check-remote-identity` (uuid/label/created byte-identical across a mock-remote round-trip; cache still refreshes). **(3) install.sh re-run marker wipe**: preflight never checked for an existing `.claude/.skeleton-version`; merge mode recorded hashes only for files copied this run and `write_version_marker` regenerated the uuid unconditionally — a no-op re-run wiped the marker to fresh-uuid/near-zero baselines. preflight now **refuses** on an installed target, naming both the redirect (`bash scripts/update.sh`) and — per the guard-rails locked principle, no protection without a stated way outside — the explicit escape hatch (delete the marker first and re-run, with the plain warning: new identity, reset baselines, orphaned shared-memory history); a guarded repair path, if ever wanted, is a future scoped flag. Guard: `install-rerun-refuse` (refusal exit + message asserts redirect AND escape hatch + marker byte-identical; second leg proves the redirect — `update.sh`'s NEW class re-adds a deleted file with the uuid stable). Consequence: `merge-add` and `replace-with-yes-piped` reworked to install.sh's remaining legitimate surface (first install into **marker-less** targets — collision skipped / custom preserved / missing added / marker created; YES-pipe overwrite against pre-seeded content); `fresh-refuse` passes unchanged. All three new scenarios wired into `ci.yml` in the same commits as their fixes (Phase 60 rule, verified via `git show --stat`). **Flagged for the queued docs-truth-sweep phase, not fixed here:** `docs/INSTALLATION.md` still advises re-running `install.sh` (update-table intro + top-level-file refresh note) — stale as of fix 3. Three commits (A: update.sh fixes + scenarios; B: install.sh guard + scenario + reworks; C: this entry). No VERSION bump.

- Phase 61 recon/plan-gate boundary directive — sharpens the approval-gated-autonomy line where Phase 60 slipped through it: on a plan-gated brief, recon slid straight from read-only reads into edits without entering plan mode, disclosed only after. Root cause: "recon" was never scoped, so a continuous read-then-edit flow never trips the gate on its own. New H3 `### Recon is read-only; the first edit is gated` in `CLAUDE_MANAGER.md`'s Strategic-judgment-patterns cluster (both mirrors byte-identical), placed with the plan-mode-discipline H3s (after `Halt on material uncertainty`, before `Plan amendment behavior`): recon is strictly read-only (`Read`, `Grep`, `Glob`, `git log/show/status/diff`, equivalents that observe without mutating); the **first mutating operation** — any `Write`, `Edit`, file creation, or state-changing `Bash` — requires plan mode entered and approved first. Reading to build the plan is thinking (autonomous); editing is action (gated); the seam between them is the approval gate. No near-complete-spec exception — a complete spec makes the plan short, it doesn't waive it. The recoverability caveat is the load-bearing reason, not a softener: Phase 60's edits were docs-only and reversible, but the rule exists so the same flow can't cross the line silently on code/config/destructive work. Additive sharpening — no existing gate language weakened. Docs-only; no VERSION bump.

- Phase 60 CI-claim verification directive — codifies the commit-discipline rule that Phase 59 surfaced by fixing Phase 57's dropped `ci.yml` wiring (the CHANGELOG's "wired into ci.yml" outlived a partial commit whose scoped `git add` omitted the workflow file, so the guard never ran). New H3 `### Verify wiring claims against the commit, not the working tree` in `CLAUDE_MANAGER.md`'s commit-discipline surface (sibling of `### Test-fixture updates land with the file change`, both mirrors byte-identical): any phase claiming a file was wired into CI — or added to a workflow, manifest, or config — confirms that file appears in the commit's own diff (`git diff --cached` pre-commit, or `git show --stat <commit>` post) before the claim is written; staged ≠ committed, working-tree-edited is neither; the Phase 57 → 59 case is cited as canonical. Docs-only, general commit-discipline rule (not a Phase-57 footnote). No VERSION bump.

- Phase 59 `update.sh` match-rebaseline — closes the stale-but-matching marker class (chip `task_715e1e30`, resolved here). Since the `a826142` backfill fix, dry-runs surfaced a growing class of entries whose recorded baseline lagged the current template while current content hash-equaled it — a file updated in template and dogfood-mirrored in one commit. **20 entries had accumulated** (9 at discovery; Phases 53/56/58 mirror edits enlarged it as predicted). Latent defect: on the next template change to any such file, classification would report `LOCALLY_MODIFIED` for a file byte-identical to the template — the exact false-positive class Phase 52's marker design exists to prevent. Fix (in `scripts/update.sh`, skeleton-root tooling — no template mirror): the classifier branch for `baseline ≠ current AND current == template` now classifies `UNCHANGED` and stages a baseline catch-up to the current hash, guarded into the marker write by a new `RAW_BASELINE_REBASED` flag (sibling of `a826142`'s `RAW_BASELINE_BACKFILLED` — same flag-into-write-guard pattern, same visibility line, same dry-run-writes-nothing rule); the dead `LOCAL_MATCHES_TEMPLATE` bucket is removed. Applies identically in interactive and `--auto-apply` (bookkeeping at classification, not part of any accept set); backfill / ORPHAN / NEW / `LOCALLY_MODIFIED`-protection flows untouched. **The class is now transient and self-healing** — a mirror edit re-stales an entry, the next `update.sh` run catches it up, no false positive ever surfaces. Semantics refined transparently where Phase 52's rule is recorded (`CLAUDE_MANAGER.md` §Baseline semantics + drift-checker.md, both mirrors): `LOCALLY_MODIFIED` = differs from *both* baseline and current template; hash-equal-to-current-template = shipped state met = `UNCHANGED` via match-rebaseline. Regression guard `scenario_match_rebaseline` (three assertions: stale-but-matching → `UNCHANGED` + caught up; converse genuinely-modified stays `LOCALLY_MODIFIED`, never re-baselined; dry-run leaves the marker byte-identical). First application: self-targeted run caught up all 20 accumulated entries + the 1 this phase's own drift-checker edit re-staled; post-run dry-run reports zero pending, the 2 genuine `LOCALLY_MODIFIED` (settings.json, optimizer ledger) correctly preserved; live converse check passed (hand-modified file still `LOCALLY_MODIFIED`). **Also fixed a Phase 57 defect found here:** the `watchdog-transcript-resolution` CI step was authored but never committed (`e688289` staged the scenario + script but not `ci.yml`; the Phase 57 CHANGELOG's "wired into ci.yml" was aspirational) — both it and the new match-rebaseline step are now wired into `ci.yml` (CI runs explicit per-scenario steps, no `all` target). Single commit, marker refreshed through the script's own guarded path (no hand edits). No VERSION bump.

- Phase 58 session-observer retirement — governance removal with full paper trail, executing the decision on Phase 57's three-leg evidenced verdict: **never dispatched** (trigger marker unconsumed; 3 Agent-tool events in the install's life), **primary surface dead** (`docs/SESSION_LOG.md`: 5 real entries, all ≤ 2026-05-16, vs 99 bare boundary markers), **contract unimplementable** (its toolset cannot delete its own `.session-ended` trigger marker). Its scope question — what patterns recur in-session — is covered by the Phase 57-fixed `task-watchdog`. Removed: the agent definition (both mirrors), the trigger-marker write and SESSION_LOG boundary-comment append in `sessionend-observe.sh` (surgical — the hook's telemetry and shared-memory-push duties byte-preserved; hook name and settings wiring unchanged), the `.session-ended` gitignore line, the marker file, and the 99 bare markers in the gitignored SESSION_LOG (5 real entries retained). Reference sweep across every live surface: removals where the mechanism died, one-line retirement notes where context needs them; `session-observer.schema.md` **keeps its name** as the shared observation-layer contract (naming note added; renaming would churn every producer for zero behavior); producer lists rewritten to the real set (task-watchdog, cruft-checker, code-quality-auditor, session-end telemetry); historical records (CHANGELOG, audits, handoff, observation data, ledger evidence) and the distinct v3+ `meta-session-observer` untouched; `task-watchdog.sh` untouched per Phase 57 protection (one comment ref flagged, accepted). Marker entries dropped via `update.sh`'s own orphan flow (baselines 72→71; CI fixtures 72→71 in-commit, three sites). Verified: post-edit hook run writes zero markers while telemetry still executes; adversarial sweep review — no blockers, 4 prose repairs applied (incl. two stale field counts and a broken-by-swap header sentence). **Re-entry path:** in-session pattern observation returns as a NEW component via normal scoping if a real Claude Code surface for it ships. No VERSION bump.
- Phase 58 optimizer evidence-bar refinement — Phase 57's backlog inversion codified into `manager-optimizer.md`'s EVIDENCE-MECHANICAL property (both mirrors): a spot-check must verify **the property the finding claims, not a proxy for it**, and before characterizing another producer's output as a false-positive class, **reproduce that producer's check from its own vantage point**. Canonical case cited in-place: the optimizer's first pass verified flagged link targets existed at the repo root (proxy) instead of resolving them from the referencing file (the property heuristic i actually checks), mischaracterizing 13 true positives as a false-positive class — inverted by Phase 57. No VERSION bump.

- Phase 57 observation-layer diagnostic — silent producers + backlog, diagnosis-first. **task-watchdog (FIXED):** had never processed a session in the install's life (marker absent; confirmed by traced run). Root cause: transcript-directory resolution dead at both layers on Windows — `encode_cwd` dropped the drive colon and never normalized the Git-Bash-form `CLAUDE_PROJECT_DIR` the hook actually receives (`/c/Users/…` → `-c-Users-…` vs CC's real `C--Users-…`), and the cwd-match fallback read only each transcript's first line, which carries no `cwd`. Fixed: encode now mirrors CC's real encoding (every non-alphanumeric → `-`, drive normalization first); fallback scans a bounded 40 lines with normalized-path comparison; `find -printf` replaced with portable `ls -t` (GNU-only, silently empty on macOS); per-dir OSError isolation; python/python3 resolved by **execution-validated** probe (the Windows Store alias stub passes `command -v` but exits nonzero); resolution misses now surface one notice line — fail-soft, never again fail-silent. **Verified live: the first observations in the install's history** (3 `recurring_failure` patterns from the real prior session, incl. ×13 plan-mode rejections). Regression guard `watchdog-transcript-resolution` wired into ci.yml — clone-as-project-dir so `OBS_DIR` stays sandboxed, fixture `cwd` generated through the same argv channel python sees (MSYS converts Unix-form argv when invoking `python.exe`), red-before/green-after demonstrated. Adversarial review found 2 scenario blockers + 1 encode should-fix pre-commit; all fixed. **session-observer (VERDICT, no code change):** requires a surface that is not populated — never dispatched (marker unconsumed; 3 Agent events ever) AND `docs/SESSION_LOG.md` holds 5 real entries (all ≤ 2026-05-16) vs 99 bare markers; a live confirmation dispatch scanned an empty window, emitted nothing, correctly skipped its resolve pass, and surfaced a third defect: its toolset cannot delete its own `.session-ended` marker, so its documented contract is unimplementable as-is. **Flagged for retire/defer — user decision, not made here.** **Backlog (16 → 0):** the optimizer's "false-positive class" verdict inverts on inspection — heuristic i resolves links doc-relative (correct markdown semantics; GitHub renders the same), and the 13 flagged links in `docs/AUDIT-v1.1.4-cc-side.md` were written repo-root-relative, i.e. genuinely broken as rendered (the optimizer's spot-check verified existence-at-root, not resolution — record corrected). Fix = 17 mechanical link repairs (`../` class) in the doc, heuristic untouched; the 3 remaining were heuristic-vii true positives ("9-field" → 10-field at `task-watchdog.md:70` both mirrors + `cruft-checker.md:3`, dogfood-only file). All 16 resolved by the codified path (`resolve_untouched()` full-scan pass on manual cruft-check dispatch); immediate re-run emits zero new instances — class closed, no bulk deletes. Two commits (producer fix + guard; doc repairs + changelog). No VERSION bump.

- Phase 56 artifact-fit-analyzer (v1.2.0 sibling) — second v1.2.0 per-project component (numbered 56: the brief said 55, but Phase 55 was already assigned to the halt-on-uncertainty directive — renumbered at plan gate, exactly the phase-ref collision cruft-checker heuristic v exists to catch). New `artifact-fit-analyzer` agent (`.claude/agents/05_meta/artifact-fit-analyzer.md`): surfaces redundancy, missing coverage, and type-misfit across the install's artifact set (agents / skills / scripts incl. drafts / commands / hooks + settings wiring) in four **closed finding lanes** — OVERLAP (verbatim excerpts + line refs from each artifact; **composition ≠ overlap**, with the safety-hooks/shared-destructive-lib pair as the canonical negative example), GAP (directive with no artifact behind it), MISFIT (absorbs the cut skill-builder/agent-builder's "should this be a skill, agent, script, or command?" recommendation), ORPHAN (zero references + untouched since install, git-evidence only; "untouched" defined incl. the dogfood arrival-commit case; frequency-of-use DEFERRED — telemetry carries no args). Inputs are a closed list; transcripts, usage counts, and model self-assessment excluded (ban per `experiments/confidence/ANALYSIS.md`). Output = capture files per `workflow-suggester.schema.md` — enabled by a new **"Non-observation producers" annotation** in the schema (both mirrors; zero field/enum changes, amendment-verified): deterministic `source_pattern_id` (sha256 over lane + sorted project-relative forward-slash paths, exact derivation in the agent def incl. the GAP-lane input), `source_pattern_type: other` + `suggested_artifact_type: manual_action` (both existing values, v1.1.4 reuse precedent), intentionally-dangling observations FK, `path:line + verbatim excerpt` evidence substitution. `confidence` is **mechanical, not judged**: high = every citation verified present this dispatch + deterministic cross-reference greps; med = any evidence line rests on inference. Clean-bill / insufficient-signal is an allowed, expected output. New `## Artifact-fit analysis (Phase 56)` H2 in `CLAUDE_MANAGER.md` + template mirror (dispatch triggers; scheduled cadence explicitly deferred to infrastructure-auditor; approved `manual_action` captures execute as user-directed edits; boundary lines vs cruft-checker / manager-optimizer incl. loop pruning / code-quality-auditor). Pre-commit 3-reviewer adversarial workflow found 3 real defects — unsatisfiable Evidence body convention for non-observation captures, non-deterministic id derivation (Windows backslash paths would break idempotency), undefined "untouched since install" for the dogfood case — all fixed pre-commit. **Verified by a first live dispatch against real dogfood state**: 1 OVERLAP capture drafted with high confidence — the analyzer caught that its own ORPHAN lane duplicates `self-audit-helper`'s drift category 1 (a self-referential first finding, exactly the job); GAP / MISFIT / ORPHAN clean bills naming every check; the mandatory negative spot-check passed (safety-hook pair evaluated and NOT flagged, composition rule applied on both prongs); zero writes outside `.claude/captures/` (tree-snapshot diff). No hooks, no SessionStart lines, no telemetry changes. Dogfood mirrors byte-identical; CI fixtures 71→72 in-commit (three sites); marker refreshed via self-targeted `update.sh`. Three-commit cadence. No VERSION bump. Surface: TV / EoG / Pinball pick up via `bash <skeleton>/scripts/update.sh`.
- Phase 53 per-project manager-optimizer (v1.2.0 centerpiece, v1) — the optimizer named in ROADMAP's b65444f bullet ships. New `manager-optimizer` agent (`.claude/agents/05_meta/manager-optimizer.md`): one instance per install, watching two layers — the project's **AI-decision patterns** and the human's **gate-interaction patterns** — and drafting refinements routed by the codified rule (AI findings → `CLAUDE_MANAGER.md` drafts; human/gate findings → `gate-config.json` drafts; never the other way). Inputs are a **closed list** (telemetry events/sessions + `model-pricing.json` + retier ledger; `.claude/observations/` all producers; `git log` recent window incl. gate-config change history; current `CLAUDE_MANAGER.md` + `gate-config.json`); session transcripts stay task-watchdog's surface, and model self-assessment stays PERMANENTLY BANNED as evidence (`experiments/confidence/ANALYSIS.md`). Human-layer signal in v1 derives from existing sources only (approval pacing from telemetry timestamps, overrides/blocks where observations record them, revert/amend from git) — thin signal produces an explicit **insufficient-signal report**, never a fabricated finding; dedicated gate-interaction instrumentation deferred pending the reviewer-behavior experiment. New `.claude/telemetry/optimizer-proposals.json` ledger (`id`/`timestamp`/`target`/`finding`/`mechanical_evidence[]`/`draft`/`status draft|approved|applied|rejected`) with the apply-after-approval flow: approved gate_config drafts = mechanical value edits the user asks CC to apply; approved claude_manager drafts = user-directed doc edits; `applied` only after the edit lands; nothing auto-applies, ever. Wiring extends the EXISTING chain in place (no new hook files, no settings changes): `sessionend-cost-proposals.sh` generalizes the Phase 48 fold to a two-family table (retier/`created_at`, optimizer/`timestamp`; poison→`*.malformed`, deterministic ids preserved) + increments the `sessions_since_last_run` counter in gitignored runtime `optimizer-state.json`; `sessionstart-cost-summary.sh` adds the cooldown-gated nudge (≤1 additional ambient line; fires on pending drafts or the `run_every_sessions` threshold) with stanza-scoped guards (adversarial-review catch: stanza-1 inputs must not suppress the nudge — fixed + fixture-verified). `gate-config.json` gains the `optimizer` block (`enabled`/`run_every_sessions`/`nudge_cooldown_sessions`, schema v2). New `## Per-project manager-optimizer (Phase 53 …)` H2 in `CLAUDE_MANAGER.md` + template mirror. **Verified by a first live dispatch against real dogfood state** (17 session rollups, 2,048 events, 152 observations, 100-commit window): 3 staged draft proposals with file-grounded mechanical evidence — incl. catching that resumed-session lineages double-count the naive 7-day spend ($1,037 naive vs $216 true delta) — plus an explicit insufficient-signal statement for friction/operation_tiers; directive-surface hashes byte-identical pre/post dispatch (zero-writes check). Pre-commit 3-reviewer adversarial workflow: 1 hook defect found, fixed, fixture-verified. Dogfood mirrors byte-identical; CI fixtures 69→71 in-commit (all three count sites); dogfood marker refreshed via self-targeted `update.sh` (the a826142 backfill path, 69→71). Three-commit cadence. No VERSION bump — v1.2.0 tags when the tier completes. Surface: TV / EoG / Pinball pick up via `bash <skeleton>/scripts/update.sh`. *[Phase 63 correction: both "fixture-verified" checks in this entry were ad-hoc pre-commit fixtures that never landed in scenarios.sh — the telemetry/cost chain's first CI regression guards are Phase 63's.]*
- Phase 48 token-cost monitoring + gate/friction config surface — Phase 46 telemetry gets its first consumer. New `token-cost-monitor` agent (`.claude/agents/05_meta/token-cost-monitor.md`) reads session telemetry + the new static pricing reference `.claude/telemetry/model-pricing.json` (per-MTok prices incl. the Sonnet 5 intro window + cache read/write multipliers, `as_of` 2026-07-02, refreshed via normal updates) and surfaces spend ambiently; re-tier proposals stage as drafts against the new `.claude/telemetry/retier-proposals.json` ledger. New `.claude/gate-config.json` is the gate/friction config surface named in ROADMAP's per-project manager-optimizer bullet: the `cost` block is its first live content (assumed_model + per-session/7-day USD warn thresholds); `operation_tiers` + `friction` ship as documented empty slots for the v1.2.0 optimizer (its second consumer, approval-gated). Four design properties locked and recorded in the components themselves: EVIDENCE-MECHANICAL (model self-assessment PERMANENTLY BANNED as an evidence source; recorded basis `experiments/confidence/ANALYSIS.md`, zero-variance RED verdict 2026-07), DRAFT-ONLY (no code path modifies a pin, settings, or any live component; applying is out of scope), NON-INTERRUPTING (ambient lines + batched drafts; nothing mid-flow), BATCH-AT-SEAMS (SessionStart hook `sessionstart-cost-summary.sh` prints ONE ambient spend line — live dogfood readout: ~$250/session, ~$971/7d at opus-4-8 rates; SessionEnd hook `sessionend-cost-proposals.sh` folds staged `retier-*.draft.json` into the ledger: deterministic ids for id-less drafts, consumed files removed only after the ledger write lands, unparseable drafts kept as `*.malformed`, idempotent, always exit 0). Wiring is SessionStart/SessionEnd hook entries only (`"type": "command"` per the v1.0 silent-inert lesson; never cron/`claude -p` — billing-pool constraint). New `## Token-cost monitoring + gate/friction config (Phase 48)` H2 in `CLAUDE_MANAGER.md` + template mirror codifies the properties and the routing rule: AI-directive changes → `CLAUDE_MANAGER.md`; human/gate adjustments → `gate-config.json`. Pre-commit adversarial 3-reviewer workflow found 1 blocker (a third CI count site: the raw-baseline-migrate assert) + 4 should-fix hook defects — all fixed and re-tested against poison fixtures. Dogfood mirrors byte-identical; CI fixtures bumped in-commit per the Phase 46c rule (63→67 components, 67→69 hooks, all three count sites). Three-commit cadence (A components / B wiring+mirrors / C changelog). No VERSION bump. Surface: TV / EoG / Pinball pick up via `bash <skeleton>/scripts/update.sh`. *[Phase 63 correction: "re-tested against poison fixtures" refers to ad-hoc pre-commit fixtures that never landed in scenarios.sh — the telemetry/cost chain's first CI regression guards are Phase 63's.]*
- Phase 55 halt-on-uncertainty manager directive — codified an explicit, load-bearing anti-fabrication directive in `CLAUDE_MANAGER.md`'s "Strategic judgment patterns" H2 (new H3 `### Halt on material uncertainty`, landed between `Empirical audit before trusting brief diagnoses` and `Plan amendment behavior` — narrow → general fabrication-prevention pair). Audited the file first: no existing halt-on-uncertainty / ask-don't-fabricate directive; the closest analog (`Empirical audit before trusting brief diagnoses`, L84–86) is scoped to verifying phase-brief hypotheses, not the broader "if you're about to assert a fact, API, file path, line number, command flag, schema field, or citation you cannot verify — stop." Gap real, fix warranted. The new H3 has three paragraphs: (1) the directive itself — STOP and read or ask rather than fabricate, with `Why` (a fabricated claim in a diff/brief/SESSION_LOG compounds downstream) and `How to apply` (grep the source; if unreachable, ask one focused question naming the specific claim); (2) explicit MATERIAL-only scoping that names the exclusions (trivial / reversible / low-blast-radius) and cites `Flow + safety, both` above as the governing rule, with a concrete contrast (guessed variable name in a draft = not material; guessed file path in `git rm` = material) — preserves flow, prevents clarifying-question avalanches; (3) interaction notes — plan-mode is the named seam (surface uncertainties in the plan so the approval gate disposes pre-execution), and `/goals` (v1.1+ future hook) is the upstream request-level catch (its absence does not waive the directive; until it ships, ask inline in plan mode); names the counter-pattern (hedged-but-confident text about a load-bearing claim → convert hedge to verification step or question). `CLAUDE_MANAGER.md` + byte-identical `template/` mirror edited in the same commit (dogfood mirror invariant). Single doc-only commit; no scripts, no schema, no fixture-count change, no VERSION bump.
- Phase 47e share-enable / share-disable confirmation-read fix — the typed-confirmation `read` in `share-enable.sh` (and `share-disable.sh --purge-remote`) read from fd 0; when stdin wasn't the keyboard (redirected / disconnected — the controlling terminal not being fd 0), `read` hit EOF instantly and self-cancelled ("cancelled — share mode NOT enabled") without ever waiting, blocking share-enable on every install. Root-caused + reproduced in plan mode (`share-enable.sh url </dev/null` → instant cancel; piped `enable` → succeeds): stdin not a TTY there. Fixed with a tty-aware read — stdin when it's a TTY (interactive) or a pipe (`[ -p /dev/stdin ]`; the `/share-enable` slash command pipes `enable\n`, CI scenarios pipe likewise); else the controlling terminal `/dev/tty` when stdin is redirected/disconnected but a terminal exists; else stdin → EOF → fail-closed cancel (unchanged). The pipe check precedes `/dev/tty`, so the piped slash-command / CI answer is never stolen by a terminal read (no hang) — satisfying the both-paths constraint. Required words unchanged (`enable` / `purge`); only the read source is fixed. Both scripts + byte-identical `template/` mirrors. Verified: piped `enable` enables, piped `nope` and `</dev/null` both fail-closed cancel, full scenario suite green (its share scenarios pipe the answer, exercising the pipe branch); the interactive `/dev/tty` wait is confirmed manually in a real terminal (no pty available to auto-test it). Out of scope (noted, not fixed): `/share-* <path>` opening the `/bug` dialog is a Claude Code client slash-parsing behavior — no skeleton-side cause found. Single commit; no VERSION bump.
- Consolidation (a) dogfood marker hygiene — brought the skeleton's own `.claude/.skeleton-version` current using `update.sh`'s own backfill logic (self-targeted run, prompts EOF-defaulted so `settings.json` intentional drift is kept; **marker-only change**). The dogfood marker was stale (last refreshed Phase 32b — the skeleton self-updates via direct commits and never ran `update.sh` on itself): `commit` pinned to `bba2d8a2`, `files` at 45, `raw_template_baselines` absent (Phase 52 never reached it), and the 47a identity fields (`install_uuid`/`install_label`/`install_created`) all missing. Ground-truthed the real file first (the handoff claim was unverified); confirmed stale, then ran `bash scripts/update.sh` self-targeting the dogfood. Its migrations did the work — `migrate_raw_baselines` recovered 45 baselines from the recorded commit + `classify` wrote current-template fallbacks for the ~18 newer files (= 63), `migrate_install_identity` generated a UUIDv4 `install_uuid` + `install_label` + ISO-8601 `install_created`, and `write_version_marker` refreshed `commit`→HEAD and `files`→63. No hand-authored field values. Verified: 63 `files` + 63 `raw_template_baselines` (all 64-char hex), identity well-formed, `version` 1.1.4, `commit` = HEAD; idempotent (re-run byte-identical); full scenario suite green; `git status` showed only the marker. Skeleton-internal hygiene only — no `template/` changes, no other installs touched, update-neutral; no VERSION bump.
- Phase 47d cross-project git memory — `/graduation-review` maintainer report (**skeleton dogfood only** — not shipped to installs; no `template/` counterpart, no mirror, like `cruft-check.sh`). New `.claude/scripts/graduation-review.sh` + `.claude/commands/graduation-review.md` read the cross-install shared-memory events and print a grouped review for manual graduation pattern-spotting: a per-install summary (label + uuid, event-file counts per producer), captures grouped by `suggested_artifact_type` (shipped only, with which installs), and observations grouped by `pattern_type` — each group with an overlap line stated honestly against the 15-install / 75% graduation threshold (always "not yet" below it; the report never implies readiness). Telemetry events are excluded. Reuses 47c-1's `smg_ensure_clone`/`smg_pull_rebase` + 47b/c's JSON envelope to reach the data; **read-and-report ONLY** — drafts no captures, promotes nothing to `template/`, makes no suggestions; the maintainer does the pattern-spotting. Share off / no clone → clean "nothing to review", exit 0. Two-commit cadence (A: command + script + 2 scenarios + ci.yml · B: CHANGELOG + CLAUDE_MANAGER note). Two new CI scenarios (`graduation-review-report` against a synthetic 3-install fixture — varying overlap, a `draft` capture that must not count as shipped, a telemetry event that must be excluded; `graduation-review-disabled`); the report scenario copies the dogfood-only script into the test install since `install.sh` won't (the libs ship in `template/.claude/lib/`). CLAUDE_MANAGER "Dogfood mirror invariants" example extended to cite `/graduation-review` alongside `cruft-checker` as a dogfood-only artifact (mirror-safe pattern doc). **Update-neutral**: adds no `template/` files — marker count unchanged (`verify_marker`/`raw-baseline-migrate` untouched); no TV/EoG/Pinball `update.sh` needed. No schema changes; no VERSION bump (47 arc bumps at v1.5). Completes the Phase 47 cross-project-memory arc (47a identity/opt-in · 47b producers/redaction · 47c-1 push layer · 47c-2 preview/purge · 47d maintainer review).
- Phase 47c-2 cross-project git memory — `/share-preview` dry-run + `/share-disable --purge-remote` (completes the 47c arc; two thin commands over existing machinery — no changes to 47b's producer or 47c-1's git layer). `--preview` is a third mode on `shared-memory-push.sh` (mode handling generalized from a `MANUAL` flag to `auto|manual|preview`): runs the real flow (ensure-clone → `pull --rebase` → producer) but stops before commit/push and reports what the next push would include — count + per-producer breakdown + a sample path — then discards the working changes (remote untouched). Mirrors the on-change gate so the report is exact by construction; reports not-enabled + exits 0 when share is off. New `/share-preview` command. `--purge-remote` extends 47a's `share-disable.sh`: typed `purge` confirmation (EOF-fail-closed, modeled on `/share-enable`), then clone the remote, `git rm` THIS install's uuid-keyed paths (`<clone>/*/<uuid>` — per-producer subtrees + `installs/<uuid>/`; the `*` glob skips dotted dirs + other installs' uuids), push the deletion via 47c-1's `smg_push` (bounded retry, fail-soft), THEN disable. Order matters: deletion pushed first; on success → disable; on a failed push → report + leave the feature ENABLED for retry; nothing-ever-pushed → clean no-op → disable normally. Plain `/share-disable` keeps its 47a behavior (stop pushing, leave remote data, no confirmation). `share-disable.md` documents both paths. CLAUDE_MANAGER share section refreshed to current state (47c-1 push + `/share-push` + `/share-status` last-push reporting; 47c-2 `/share-preview` + `--purge-remote`), replacing the stale "no data has crossed yet / push lands in 47c" line that 47c-1's commits never updated. Three-commit cadence (A: `--preview` + `/share-preview` + 2 scenarios · B: `--purge-remote` + confirmation + 4 scenarios incl. 2nd-install isolation + EOF-fail-closed · C: CLAUDE_MANAGER + CHANGELOG). Six new CI scenarios (`share-preview-enabled`/`-disabled`, `share-purge-remote`/`-nothing`/`-confirm-eof`, `share-disable-plain`); `verify_marker` + `raw-baseline-migrate` 62→63 (one new install-counted template file: `share-preview.md`). No schema changes; no VERSION bump (47 arc bumps at v1.5). **Follow-up (flagged, deferred):** 47c-1's on-change gate ignores the whole `version/` path, not just `version.json`'s `event_timestamp` — a real `version`/`commit` bump with zero other activity wouldn't push until another event rides it along (telemetry emits a new event each active session, so this is a degenerate-case lag). Surface: TV / EoG / Pinball pick up the new command + edited script on the next `bash <skeleton>/scripts/update.sh` run.
- Phase 47c-1 cross-project git memory — git push layer + SessionEnd cadence (the transport for 47b's data-movement layer; 47b wrote redacted events into a LOCAL tree, 47c-1 syncs that tree to the remote). `.claude/shared-memory/` becomes a working clone of the `share-config.json` remote. New `.claude/lib/shared-memory-git.sh` (sourced): `smg_ensure_clone` (idempotent — plain-clone an empty dir / graft `.git` into a populated dir / no-op an existing clone; checks `<dir>/.git` and deliberately NOT `git rev-parse --is-inside-work-tree`, which false-positives on a plain dir nested inside the parent repo and would let git ops fall through to the parent — i.e. commit/push the project itself), `smg_pull_rebase` (skips an empty remote — no upstream yet), `smg_push` (stage → commit-if-changed → push → on a non-fast-forward rejection `pull --rebase` + retry, bounded ×3, fail-soft; sets a clone-local commit identity when none is configured so CI runners / fresh clones can commit). New `.claude/scripts/shared-memory-push.sh` orchestrator: no-op when share is off; else ensure-clone → `pull --rebase` → run 47b's `shared-memory-produce.sh` (CALLED, not modified) → push on-change → record `.claude/.last-shared-push` (timestamp + cumulative tracked-file count). On-change gate ignores `version.json` timestamp churn (the 47b envelope stamps `event_timestamp` every run and version is current-state/overwritten) — an unchanged session pushes nothing and reports "Nothing to push", and the churn is discarded to keep the tree clean for the next `pull --rebase`. Always exits 0 — wired into `sessionend-observe.sh` after the telemetry call; a failed/unreachable push NEVER blocks or delays session end, the next SessionEnd catches up. Pure git/bash — no `claude -p`, no model-quota surface. New `/share-push` command (`shared-memory-push.sh --manual`): the Q3-lock on-demand fallback — same orchestrator, same on-change gate, same fail-soft, only changes WHEN the push runs; emits user-facing status. `/share-status` extended to report last-push time + cumulative file count. 47a's `share-enable.sh` push trio swapped to `smg_push` (one push mechanic, one place — closes the no-retry handoff loose end). `.gitignore` excludes the `.claude/.last-shared-push` marker (per-install runtime, same rationale as the shared-memory tree). Three-commit cadence (A: git layer + `smg_push` + share-enable hardening + `share-push-empty-remote`/`share-push-race` scenarios · B: orchestrator + SessionEnd wiring + `/share-push` + share-status + 4 scenarios + the two integration fixes [`smg_is_clone` parent-repo false-positive; `smg_push` clone-local identity] found while wiring · C: CHANGELOG). Six new CI scenarios (`share-push-empty-remote`, `share-push-race`, `share-push-enabled`, `share-push-failsoft`, `share-push-disabled`, `share-push-manual`) wired into `ci.yml`; count-sensitive fixtures bumped — `verify_marker` 59→62 (both sites) and `raw-baseline-migrate`'s `raw_template_baselines` assertion 59→62 (+3 install-counted template files: `shared-memory-git.sh` + `shared-memory-push.sh` + `share-push.md`). No source-schema changes (47c-1 owns git transport only — does not modify 47b's producer or any schema; no `privacy_class`/`shipped_at`/`rejected_at` added). No VERSION bump (47 arc bumps at v1.5; stays [Unreleased]). 47c-2 (dry-run + opt-out commands) deferred. Surface: TV / EoG / Pinball pick up the new lib + script + command on the next `bash <skeleton>/scripts/update.sh` run.
- Phase 47b cross-project git memory — producer integration + `redact-capture.sh` (data-movement layer; writes events into a LOCAL tree only — no git push, that's 47c). New `.claude/lib/shared-memory.schema.md` defines the uniform JSON envelope every producer emits (`schema_version` 1; marker-native `version`/`commit` — no `skeleton_*`; `producer`/`install_uuid`/`install_label`/`event_timestamp`/optional `created_at`/`payload`; tree layout `<producer>/<install_uuid>/<date>/`, version is current-state with no date dir). New `.claude/lib/shared-memory-lib.sh` (sourced) holds the shared `sm_build_envelope` builder + local-tree write helpers — spools the stdin payload to a temp file (the heredoc already occupies stdin with the python program, so payload + program can't share it) and forces LF stdout via `reconfigure` (Windows text-mode emits CRLF, breaking the LF convention the 47a writers set; same fix as `share-status.sh`). New `.claude/lib/redact-capture.sh` converts terminal-state captures (`status: shipped`/`rejected` only — draft/approved skipped via exit 2), fail-closed on malformed frontmatter (exit 3); captures carry no `privacy_class` so the body is treated as share-with-redaction and text-redacted (`*KEY=`/`*TOKEN=`, Bearer/Authorization, home paths incl. Windows `C:\\Users\\<u>\\`, base64 >32 chars, URL query strings) — models `redact-observation.sh`'s exit-code/fail-closed contract without modifying that shipped file. New `.claude/scripts/shared-memory-produce.sh` orchestrates four idempotent walkers when share is enabled: captures (via redact-capture.sh) → `captures/`; observations (via the existing `redact-observation.sh` — local-only refused, share-with-redaction field-stripped, safe-to-share passed) → `observations/`, EXCLUDING the `token_telemetry` observation so it isn't double-emitted; telemetry (the safe-to-share token-telemetry observation only) → `telemetry/`; version (current marker state, overwritten) → `version/`. Append-only producers skip a key already present under any date dir (idempotent across days); version overwrites in place. No-op when share is disabled (`share-config.json` absent or `enabled:false`) — no tree, no events. `.gitignore` excludes the local staging tree `.claude/shared-memory/` (per-install runtime, same rationale as `observations/` + `telemetry/`; target-repo `.gitignore` propagation remains Phase 49). `CLAUDE_MANAGER.md` + byte-identical template mirror + `docs/INSTALLATION.md` § Share mode opt-in document the producers, the local tree, and the envelope schema. Design locks honored: reuses (does not reimplement) `redact-observation.sh` — its strip logic is jq field-allowlisting, so the only shared code factored out is the envelope builder; no `privacy_class`/`shipped_at`/`rejected_at` added to any source schema (47b reads source artifacts only). Three-commit cadence (A: schema + lib + redact-capture + mirrors + `share-capture-redact` scenario · B: producers + `.gitignore` + docs + 3 producer scenarios + ci.yml wiring · C: CHANGELOG). Four new CI scenarios (`share-capture-redact`, `share-produce-enabled` [real opt-in flow], `share-produce-idempotent`, `share-produce-disabled`) wired into `ci.yml` with a jq probe in the env step; `verify_marker` 58→59 and `raw_template_baselines` 55→59 (+4 install-counted template files across A+B: schema.md + 2 libs + produce.sh). Plan-mode ground-truth confirmed the two brief-flagged premises: `/share-enable` leaves no persistent local tree (ephemeral mktemp clone, rm-on-exit) so 47b creates it; telemetry's only push-eligible artifact is the safe-to-share observation (raw `events/*.jsonl` + `sessions/*.md` stay gitignored-local). No VERSION bump (47 arc bumps at v1.5; stays [Unreleased]). Surface: existing installs (TV / EoG / Pinball) pick up the new libs + script on the next `bash <skeleton>/scripts/update.sh` run.
- Phase 47a cross-project git memory — identity layer + opt-in mechanic (infrastructure only; no data movement). `.skeleton-version` gains three immutable install-identity fields: `install_uuid` (UUID v4 via Python stdlib `uuid.uuid4()` — no new dependency), `install_label` (defaults to install-dir basename; hand-editable for now), `install_created` (ISO-8601). `install.sh` writes them at fresh install; `update.sh` backfills them once, inline + silent, for pre-47a markers (modeled on the Phase 52 `raw_template_baselines` migration gate; never regenerates an existing `install_uuid`). New `.claude/share-config.json` schema (created on first opt-in; absent by default = not configured; NOT part of the installed template) + three commands with `template/` + byte-identical dogfood mirrors: `/share-enable <remote-url>` (clones a user-controlled remote, initializes the shared tree on an empty bare repo, writes `installs/<uuid>/sentinel.json`, requires the literal word `enable` on stdin — fail-closed on EOF — then commits/pushes and writes share-config), `/share-disable` (flips to `enabled:false` + stamps `disabled_at`, preserves `remote_url`/`enabled_at`; `--purge-remote` deferred to 47c+), `/share-status` (read-only state report). Sentinel + future bus envelope use marker-native `version`/`commit` field names — no `skeleton_*` translation layer (Q2 retro-correction; the brief's `skeleton_version`/`skeleton_commit` premise was wrong, caught in plan-mode). Trust model is single-user-multi-install (one person's installs → one of their own remotes). Four-commit cadence (A: identity fields + install/update + CI · B: share-enable · C: share-disable/status · D: docs). Four new CI scenarios (`install-uuid-fresh`, `install-uuid-backfill`, `share-enable-fresh-remote`, `share-status-disabled-default`); `verify_marker` counts bumped 49→55 (4 new install-counted template files). NO observation/capture/telemetry data pushed to the bus yet — producer integration + redaction deferred to Phase 47b; SessionEnd push cadence deferred to 47c. No VERSION bump (internal scaffolding for a future-version capability; stays [Unreleased]).
- Extracted 4 locked architectural principles (captures-surface enum stability, Model C lesson codification, billing-pool design constraint for v1.2.0, single source of truth for safety patterns) from session handoff into `docs/ROADMAP.md` canonical Locked architectural principles section. Doc-shuffle only — no behaviour change.
- Logged v1.1.5+ pre-pinball queue in `docs/ROADMAP.md` — claude-mem install locked; pre-pinball builds, ecosystem integration decisions, `/goal` vs `/goals` naming collision, first-install plugin suggestion flow, mental-model drift as audit-priority finding; multi-session-cumulative detection and roadmap velocity flagged for audit. Doc-only — no behaviour change.
- Generated v1.1.4 state-dump artifact at `docs/AUDIT-v1.1.4-state.md` for post-v1.1.4 comprehensive audit. Doc-only — no behaviour change.
- Generated CC-side audit artifact at `docs/AUDIT-v1.1.4-cc-side.md` reviewing v1.1.4 state with code-shape + CI + permissions focus. Reconciliation with chat-side audit pending. Doc-only — no behaviour change.
- Phase 30b mechanical fixes from CC-side audit closure: amended `session-observer.schema.md` `occurrences` field for deterministic-producer carve-out (cruft-checker + code-quality-auditor MAY emit at `occurrences = 1`; pattern-detection producers retain `≥ 2`) — H1; fixed `cruft-check.sh` header heuristic count (7→9) + added confidence-tier override comment — H2/H3; added portable `timeout` fallback in `update.sh` for macOS without coreutils, plus env-overridable `SKELETON_REPO_URL` for testability — H4; extended destructive-pattern libs with 11 new patterns (bash: rsync/dd/mkfs/truncate/find-root/shred|srm shapes; PowerShell: Stop/Restart-Computer/Set-Acl/Compress-Archive/wmic shapes) — H6; added 4 CI scenarios (check-remote-cached, hook-fail-closed-bash-safety, cruft-check-fixture, replace-with-yes-piped) closing audit-surfaced CI gaps — H5/H7. Robustness fix surfaced during phase exec: PreToolUse hooks (bash + powershell, both mirrors) now resolve LIB via `CLAUDE_PROJECT_DIR` instead of relative path — eliminates the fail-closed-on-CWD-drift mode. Additive; no behaviour change to baseline producers.
- Phase 30c PreToolUse hook FP exemption: parser-aware detection of `git commit -m` message bodies + heredoc payloads in both bash-safety and powershell-safety hooks. Destructive-pattern strings appearing inside commit messages or heredoc bodies no longer trigger deny. Closes the 3rd-recurring FP (Phase 21 + 24 + 30b empirical hits). Counter-tests (semicolon-chained, AND-chained, bare-pattern, post-heredoc) verify destructive patterns OUTSIDE exempted shapes still deny. Shell-portable parser — bash regex + sed only; no Python/jq in the redaction logic. Mirror byte-identical between dogfood + template for both hooks. New CI scenario `hook-fp-exemption-git-commit-message` covers 11 test cases (6 bash + 5 PowerShell).
- Phase 31 bulk codification: extracted 5 "Other principles" + 1 architectural sprint rule (Compose-with-available-surfaces) into `docs/ROADMAP.md` § Locked architectural principles (now 12 principles total); codified 2 sprint rules (empirical-audit, Co-Authored-By trailer) + hook-path discipline (Phase 30b emergent fix) in `CLAUDE_MANAGER.md` as strategic judgment patterns + a Three-commit-cadence subsection; added mission statement to `docs/STORY.md`; production-miles precision reframe in ROADMAP § v1.2.0 Gating. Simplified handoff doc: dropped now-codified principles + sprint rules sections + closed-loose-end (bash-safety FP), added "Where things live" pointer, updated TL;DR + What's-next to post-Phase-30c state; ~24% length reduction (322→244 lines). Back-mirrored template→dogfood for `self-audit-helper.md` (73→92 lines, extended Severity rubric) and `project-tuner-helper.md` (124→188 lines, --report-only mode + Output contract). Cross-reference triangle complete (ROADMAP ↔ CLAUDE_MANAGER ↔ STORY; handoff "Where things live" points to all three). Doc-only — additive on canonical surfaces, no behavior change to producers; back-mirrors add capability only.
- Phase 32a marker refresh dry-run: ran `bash scripts/update.sh --dry-run` against v0.4 fossil dogfood marker; captured six-way classification + BACKFILL_MODE detection in `docs/AUDIT-v1.1.4-marker-refresh-dryrun.md`. No marker write, no file write — read-only inventory only. 4 files surfaced as TEMPLATE_UPDATED; 3 are line-ending FP (CRLF template vs LF dogfood) and 1 is locked intentional drift (settings.json `defaultMode` + `compactPrompt` + cruft-check.sh SessionStart entry). Findings inform Phase 32b approach (deferral preserves fossil as intentional dogfood lag; apply with `[S]kip all` on TEMPLATE_UPDATED produces marker refresh without clobbering locked drift). Doc-only — no behavior change.
- Phase 32c line-ending normalization: tightened `.gitattributes` to enforce LF on text files via `* text=auto eol=lf` catch-all rule (preserves existing narrow rules for shell scripts + hooks/scripts markdown); stripped CRLF in worktree for 3 drifted agent files (`audit-helper.md`, `monitoring-helper.md`, `session-observer.md`, all under `template/.claude/agents/`). Closes 3 of 4 Phase 32a TEMPLATE_UPDATED entries — verified by re-running `update.sh --dry-run` post-fix (TEMPLATE_UPDATED reduced from 4 to 1; only locked `settings.json` drift remains). Compresses Phase 32b risk surface from 4 candidates to 1. Doc-only — agent content unchanged, only byte representation.
- Phase 32b marker refresh apply: ran `update.sh` interactively with safe defaults (skip-all on the 1 TEMPLATE_UPDATED entry `settings.json`; no other prompts fired since other buckets empty per Phase 32a/c); refreshed `.claude/.skeleton-version` from v0.4 fossil to v1.1.4 current schema with per-file SHA-256 hashes for 45 tracked files (brief predicted 44; empirical count is 45 — `update.sh` `classify()` silent-backfill records `hash_current` for ALL template files including the skipped settings.json). Marker now carries real baselines (vs cache-empty silent no-op state on the fossil). `drift-check.sh` post-apply correctly reports `installed: 1.1.4` (was implicit 0.4.0); cache-empty notice persists until a future `update.sh --check-remote` run populates `cached_skeleton_head`. Closes marker-refresh arc (32a inventory + 32c line-ending normalization + 32b apply). Settings.json intentional drift preserved at recorded baseline = dogfood-resolved bytes (future template updates of settings.json won't false-classify dogfood as LOCALLY_MODIFIED).
- Phase 33 directive layer alignment: removed `/goals` TEMPLATE STUB from `CLAUDE_MANAGER.md` strategic judgment patterns (audit-emergent decision: don't build `/spec`; use ecosystem `feature-dev` + `superpowers` brainstorming once bundle installs). Reframed `integration-checker` H3 from TEMPLATE STUB to `STATUS: DEFERRED to v2.0` (planned v2.0; for v1.1.5+ use `code-quality-auditor` candidate mode when Phase 39 / v1.5-B ships). Added new `### When to dispatch code-quality-auditor` H3 in the dispatch-cluster with manual-vs-automatic guidance (6 dispatch H3s total). Added new `## Core vs integration boundary` H2 section enumerating 12 durable core primitives + integration-layer composition surface + boundary discipline rules. Both `CLAUDE_MANAGER.md` (dogfood) + `template/CLAUDE_MANAGER.md.template` byte-identical post-edits modulo documented placeholder drift. Added `CLAUDE_MANAGER.md` to `EXEMPT_PHASE_FILES` in `cruft-check.sh` (directive layer intentionally forward-references future phases — same precedent as audit artifacts). Closed obsolete `.skeleton-version dogfood lag` loose-end in handoff (marker current post-Phase-32b). Doc-only — no behavior change.
- Phase 34 bundle install (5 of 6 ecosystem plugins): installed `feature-dev`, `code-review`, `commit-commands`, `security-guidance` (trust-tier-1, Anthropic-official from `claude-plugins-official`) + `superpowers` (trust-tier-2, `obra/superpowers-marketplace`) via `claude plugin install`. Mid-execution mechanism switch from interactive `/plugin install` slash commands to autonomous absolute-path `claude.exe plugin install` (GitHub #42142, #56623, #18088 — slash form unavailable in Claude Code Desktop). All 5 installs clean: `plugin-quality-check.sh` zero findings against each newly-cached plugin; no `installed_plugins.json` vs `enabledPlugins` enablement gap (GitHub #20661 did not repro on 2.1.138 with `--scope user`); no PreToolUse hook conflicts (security-guidance matches `Edit\|Write\|MultiEdit`, skeleton's safety hooks match `Bash\|PowerShell` — disjoint); 1 new SessionStart hook from superpowers joins skeleton's chain (additive, not blocking). `claude-mem` deferred to Phase 34b (eyes-open install pending) per HALT-ON-CONCERN findings: `npx claude-mem install` (primary path) auto-installs Bun + uv globally; background Bun worker daemon; opt-in network egress to Telegram/Discord/Slack code path; OpenClaw-related `curl | bash` install pattern. The 4 concerns documented verbatim in `docs/PLUGIN-INSTALLS-v1.1.4.md` § Trust-tier-2 vetting as Phase 34b's input contract. Added `PLUGIN-INSTALLS-v1.1.4.md` to both `EXEMPT_VFILES` (heuristic x — multi-version refs) and `EXEMPT_PHASE_FILES` (heuristic v — forward-refs Phase 34b, 35, 36) in `cruft-check.sh`. Skeleton-repo footprint: 3 files (artifact NEW, cruft-check.sh, CHANGELOG); plugin state in `~/.claude/plugins/` is global filesystem state, not committed.
- Phase 34b claude-mem eyes-open install (Phase 34 deferral closure): installed `claude-mem@thedotmack v13.2.0` via hybrid path — marketplace install (`claude plugin install claude-mem@thedotmack`) for clean plugin manifest tracking + `npx claude-mem@latest install` for runtime bootstrap. Plan-mode investigation surfaced marketplace path now works (issue #1170 closed 2026-02-18 by repo author); pre/post diff captured verbatim across tooling, shell rc, plugin state, processes, global tool dirs. Branch (c) materialized at Step 4: marketplace install clean (no Bun/uv side effects, no shell rc edits, no daemon spawn) BUT `version-check.js` (Setup hook target) emitted `claude-mem: runtime not yet set up - run: npx claude-mem@latest install`; user opted for hybrid Option A. Bootstrap first attempt failed mid-stream with ENOSPC (disk 100% full while bun extracting 142 tree-sitter tarballs); user manually freed disk; retry succeeded. Final state: Bun upgraded 1.3.9→1.3.14 (claude-mem requires specific version), uv 0.11.14 installed at `~/.local/bin/uv.exe` (66M, not on PATH but plugin self-handles via hook PATH augmentation), MCP server `mcp-search` registered + ✓ Connected, plugin ships 12 skills (none commands/agents), aggressively-instrumented hook surface (Setup/SessionStart×2/UserPromptSubmit/PostToolUse(*)/PreToolUse(Read)/Stop). **Worker daemon autostart explicitly skipped in non-TTY environment** ("start it manually with `npx claude-mem start`") — Concern 2 mitigated by default. Auto-memory disabled (CC env has `CLAUDE_CODE_DISABLE_AUTO_MEMORY=1`). All 4 Phase 34 concerns dispositioned empirically in artifact § Phase 34 concerns disposition. Rollback procedure documented with exact commands. Establishes canonical "vetted-with-concerns → eyes-open install" pattern for v1.5-tier plugins (Phase 38+); claude-mem is the first execution. Skeleton-repo footprint: 2 files (artifact append + CHANGELOG); plugin state + ~/.claude-mem/ + ~/.local/bin/uv.exe + Bun upgrade are global filesystem state, not committed.
- Phase 35a handoff refresh: comprehensive snapshot through Phase 34b at commit f72d132. Audit-emergent queue closure documented (9 phases shipped 30→34b over 2 days). 10 new phase recaps in Sprint state section. New `## Architectural findings from this sprint` H2 section captures audit-as-primitive validation, bash CLI form, plugin/skeleton scope separation, "opt-in everything" UX principle, hook-path discipline, eyes-open install pattern, and strategist-chat-layer scope (meta-system only). New v1.1.5 onboarding tier (5 phases A-E) queued for post-Phase-35-close. Phase 35 evaluation window opens with suggested sequencing (pinball Day 3-7, TV Day 7-14, EoG Day 14-21). Updated loose-ends: closed bash-safety FP + dogfood lag + handoff:136; new Phase 35 watch items + disk-space pre-check + cached_skeleton_head deferred-indefinitely + strategist-chat-scope validation. Cuts updated: user-pause-on-plugin-install assumption + claude-mem indefinite-deferral. Replaces prior handoff version dated 2026-05-17. Doc-only — no behavior change.
- Codified plain-English user-facing communication discipline as new `### User-facing communication: plain English by default` H3 under `## Strategic judgment patterns` in `CLAUDE_MANAGER.md` + `template/CLAUDE_MANAGER.md.template` (byte-identical modulo placeholder resolution). Model C codification — direct lesson into the directive surface that architecturally fits, no separate lessons doc (per Phase 17/18 precedent; Models A and B explicitly rejected at Phase 18 cut). Doc-only — no behavior change.
- Phase 46c CI fixture bump + same-commit fixture-update rule codified: `.github/test-fixtures/scenarios.sh` `verify_marker 45` → `verify_marker 49` at lines 100 and 222 (the only two call sites; L51/L53 are the function def). Phase 46 added 6 template files, 4 install-counted (3 libs + 1 README; `.gitkeep` files skip per `scripts/install.sh:357`), but the fixture wasn't updated alongside — CI was red on `bd672bc` (Phase 46 docs) and `a387336` (Phase 46b session_id fix). New `### Test-fixture updates land with the file change` H3 added to `CLAUDE_MANAGER.md` + template mirror under `## Three-commit cadence` codifying the rule: when a phase adds or removes files under `template/`, CI fixture counts in `.github/test-fixtures/` must update in the same commit. No VERSION bump (mechanical CI fix + doc-only directive addition; skeleton-only — `.github/` and `CLAUDE_MANAGER.md` not template-mirrored to target projects).
- Phase 46b session_id resolution for fresh sessions (Phase 46 stub-data bug): Phase 46 smoke test surfaced a ~67% stub rate on fresh CC sessions — telemetry artifacts wrote `session_id: unknown-<ts>` with `data_available: false` and zero token counts. Root cause: `find_current_jsonl()` in `.claude/lib/generate-session-telemetry.sh` used a file-mtime heuristic to pick the "newest `*.jsonl` in the CC-encoded project dir", which races with CC's next-session placeholder creation — CC creates the new session's empty JSONL before the prior session's SessionEnd hook completes, so find picked the empty file, python found no `sessionId` events, fell through to `unknown-<ts>`. The 159fa886 session worked only because no newer placeholder existed at its SessionEnd fire moment. Fix: `sessionend-observe.sh` now reads CC's SessionEnd JSON payload from stdin once (matching the canonical `INPUT=$(cat); jq -r '.field // empty'` pattern already used by `pretooluse-bash-safety.sh` + `pretooluse-powershell-safety.sh`), extracts `session_id` + `transcript_path`, exports as `CLAUDE_HOOK_SESSION_ID` + `CLAUDE_HOOK_TRANSCRIPT_PATH` (namespaced to avoid collision if CC ever exposes `CLAUDE_SESSION_ID` natively). `generate-session-telemetry.sh::find_current_jsonl()` now prefers the env-supplied transcript path first, falls back to `$PROJECTS_DIR/<encoded>/$CLAUDE_HOOK_SESSION_ID.jsonl` second, falls back to the existing mtime heuristic third. Embedded Python accepts `CLAUDE_HOOK_SESSION_ID` as argv[7] and uses it directly — first-event discovery loop preserved as final fallback for tested env / manual invocation without env vars. Mirror byte-identical between dogfood + template for both hook + lib. Migration NOT re-run; existing `unknown-*` observation files stay as-is. Hypothesis from Phase 46b brief partially refuted in plan mode: hook never read stdin to begin with — race is in JSONL placeholder creation, not stdin consumption. Fix shape from brief remained correct. No VERSION bump. Surface: TV / EoG / Pinball need another `bash <skeleton>/scripts/update.sh` run after this lands to pick up the additional lib/hook changes on top of Phase 46.
- Phase 46 token telemetry foundation + cross-install observation schema: extended `session-observer.schema.md` + template mirror with two new fields — `privacy_class` (3-value enum: `local-only` | `safe-to-share` | `share-with-redaction`, required v1.1.5+, governs cross-install emission) and `target_resource` (format `<category>:<name>`, required on `token_telemetry`, optional/encouraged on others; categories: `agent` / `skill` / `command` / `script` / `plugin` / `hook` / `file` / `session`). 11-field schema (10 required + `target_resource` conditional + `notes` conditional). New `.claude/lib/migrate-observation-privacy.sh` + template mirror — idempotent backfill of pre-v1.1.5 observations to `privacy_class: "local-only"` (conservative default; jq -aS --indent 2 + tr -d '\r' to match Python producers' json.dump format); 132 existing observations migrated cleanly. Producer updates (4 — drift-checker exempt because it's text-only with no observations): `session-observer` → `local-only` on all 4 pattern types · `task-watchdog` (script + doc + template) → `share-with-redaction` with target_resource derived from failing tool / script basename · `cruft-checker` (dogfood-only) → `local-only` · `code-quality-auditor` (script + doc + template) → `share-with-redaction` with `target_resource: plugin:<name>` derived from signature prefix. New `.claude/lib/redact-observation.sh` + template mirror — fail-closed cross-install emission gate: `local-only` REFUSED (exit 2), `safe-to-share` pass-through, `share-with-redaction` stripped to allowlist (pattern_id, source, pattern_type, occurrences, first_seen, last_seen, resolved_at, confidence, privacy_class; target_resource ONLY when category not in {file, command}; evidence reduced to `{timestamp, kind}` per entry; notes / summary / args_redacted / tool_name DROPPED by default). New schema fields default to STRIPPED — fail-closed allowlist, not blocklist. Smoke-tested against 6 cases (local-only refuse, safe-to-share pass-through, share-with-redaction with tool: target preserved, share-with-redaction with file: target stripped, missing privacy_class refuse, nonexistent file refuse). New `.claude/lib/generate-session-telemetry.sh` + template mirror — SessionEnd-only collector that parses the current session's JSONL transcript and writes three artifacts: per-event JSONL at `.claude/telemetry/events/<session_id>.jsonl` (one line per tool call w/ turn-level tokens_in/out + target_resource), per-session markdown rollup at `.claude/telemetry/sessions/<session_id>.md` (YAML frontmatter + tool counts + top expensive turns + top subagent dispatches), and `token_telemetry` observation at `.claude/observations/token-telemetry-<session_id>.json` (privacy_class: safe-to-share, target_resource: session:<id>). Useful unit defined empirically per CORE 12: `useful_units_shipped` = git commits in session window + `useful_units_drafted` = `.claude/captures/*.md` mtime in window; `tokens_per_useful_unit` = (input + cache_read + cache_creation + output) / max(1, shipped + drafted). Cache reads INCLUDED in the headline metric — CORE 11 is about consumption, not billing. `sessionend-observe.sh` hook + template mirror extended to invoke the lib post-marker-write; lib always exits 0 (telemetry MUST NOT block the hook chain). NO PostToolUse / SubagentStop wiring per `.claude/hooks/README.md` deliberate-omission (user-level hook shadowing risk); token data comes from the JSONL transcript, same pattern task-watchdog already uses. New `.claude/telemetry/` dir tree (events/ + sessions/ + README) + template mirror; `.gitignore` extended to exclude per-session `.jsonl` + `.md` runtime files (gitkeep + README tracked). Live-tested against the executing session's JSONL: 146 turns w/ usage, 88 tool calls captured, ~27M total tokens processed, 2 commits surfaced as useful units, ~13.5M tokens-per-useful-unit (cache reads dominate raw input by 4 orders of magnitude — the in-only metric was misleadingly LOW). Telemetry IS an observation — same schema, same pipeline, same approval surface; no parallel infrastructure. Privacy enforcement is SCHEMA-LEVEL (privacy_class on observation), not collector-level. drift-checker exempt from privacy_class rollout — text-only producer, no observations to label. encode_cwd path-encoding fixed to match CC's actual scheme (`C:\Users\…` → `C--Users-…`, no colon removal); Python fallback added for path-shape divergence between hook env and tested env. Four-commit cadence (A: schema + migration + producer updates · B: redact-observation.sh · C: telemetry collectors + hooks · D: CHANGELOG + verification). No VERSION bump (lands in [Unreleased]; bumps at tier ship per Phase 44 per-component gating). Surface: TV / EoG / Pinball will need `bash <skeleton>/scripts/update.sh` runs after this lands to pick up the schema + lib changes.
- Phase 45 ROADMAP + STORY gate-anchored restructure + 4 new core principles + Phase 44 fold + time-gate strip: `docs/ROADMAP.md` reorganized around readiness gates instead of version tiers (12 H2 sections: intro + Where we are / Where we're going / How readiness gates work [NEW] / Available now / Gated on first-project signal sufficiency / Gated on v1.1.5 ship / Gated on production maturity / Gated on broad install base / Locked principles core 12 / Cuts / Closing / Appendix operational 9); 12 locked core principles inline + 9 operational principles in new Appendix; 4 new CORE principles codified (Empirical readiness, not the clock · Soft gates vs hard safeguards [with vocabulary clarification on "action": operations that modify state visible outside the assistant's scratchpad — public-info reads don't qualify] · Token-cost as design driver [token-cost agent ranks alongside manager as top-sophistication peer] · Usefulness is the floor [LOAD-BEARING — every principle and mechanism evaluated against it; token-cost optimization NEVER degrades output quality]); 1 new Appendix principle codified (Operation tier classification — Tiers 1-5, design only this phase, Phase 48 implements). 4 new Cuts entries (3-project/66% graduation threshold cut at Phase 44 · v1.2.0 monolithic tier gating cut at Phase 44 · claude-skeleton-auto as shipped product variant cut · Time-based gating across roadmap tiers cut at Phase 45). `docs/STORY.md` § Core principles reconciled to ROADMAP CORE 12 (preserved 6 — Approval-gated autonomy · Per-project governance · Multi-project graduation [threshold text updated] · Flow + safety, both · Compose, don't compete · Guard rails, configurable but transparent; removed Quality filter, not directory + Plain English by default; added 6 — Empirical readiness · Soft gates vs hard safeguards · Scope actively governed · Ever-evolving being · Token-cost · Usefulness is the floor). Multi-project graduation threshold updated everywhere to 75%+ adoption with 15-project floor (target 20+ / 90%+), stable through sufficient consecutive sessions and observation cycles. Time-gating language stripped throughout active sections (≥4 weeks / 4-week window / ≥2-3 weeks of post-v1.1.5 production miles / two weeks of miles on TV + EoG parenthetical); historical time references in Cuts entries preserved as context. ≥95% prose verbatim — section moves dominate. Three-commit cadence (A: ROADMAP / B: STORY / C: CHANGELOG). No template mirrors (skeleton-only docs). No VERSION bump — doc work, stays [Unreleased].
- Phase 44 v1.2.0 per-component gating + cross-install graduation relocation to v3+: rewrote `docs/ROADMAP.md` § Multi-project graduation threshold (3 projects → 15 / target 20+; 66% adoption → 75% / target 90%+) with prose explaining the sample-size floor against small-N coincidence promotion + v3+ relocation framing; rewrote § v1.2.0 meta-evolution gating to per-component split (per-project components gated on ANY ONE project with ≥2-3 weeks miles; pure-design components — `/goals` expanded + scheduled-goals — can open NOW; cross-install components moved out); removed `meta-session-observer` + `template-promoter` from v1.2.0 components-in-scope list; renamed § v3.0+ header to "ecosystem maturity tier (cross-install graduation + multi-LLM sibling)" + added new H3 "### Cross-install graduation machinery" housing the relocated mechanism (manual-today / formal-mechanism-at-v3+); appended new Cuts entry covering the relocation + threshold raise rationale. Reframed `docs/STORY.md` § How it prevents scope decay Stage seven (single-sentence "at least three projects" + present-tense meta-session-observer → multi-sentence manual-today + v3+ formalization framing). Updated `CLAUDE_MANAGER.md` + `template/CLAUDE_MANAGER.md.template` L376 § Template-content map → "How per-project edits flow" paragraph (byte-identical mirror parity preserved) with new thresholds + v1.2.0 → v3+ relocation pointer. Single commit (Phase 39 / 43 precedent — gating-bar amendments). Builds on Phase 35b per-project manager-optimizer reframe; doesn't relitigate it. No VERSION bump — doc-only.
- Phase 43 CLAUDE.md reframe amendments: reframed L3 dogfood tagline ("Orchestration layer on the Claude Code ecosystem" → "Structural immune system for Claude Code projects — the governance layer that keeps scope coherent over time") + added constitutional-framework anchor paragraph (CLAUDE.md as per-install constitution; pointers to ROADMAP § Locked architectural principles + CLAUDE_MANAGER.md as executive surface + audit triad as watchdog cadence; echoes STORY's three-piece governance shape) + added scope-actively-governed closing paragraph (audit triad cruft-checker / drift-checker / code-quality-auditor named explicitly with session-start-cadence framing). Closes Phase 42 audit's 1 high-impact finding + 2 coverage gaps. 2 review-needed items (L5 "useful first, philosophy second" / L25 "L0 / L1 / L2" jargon) deferred to future tone pass per chat triage. Mirror `template/CLAUDE.md.template` byte-identical to dogfood modulo 6 placeholders (L1, L3, L5, L11, L17, L23 post-edit — shifted +2 from prior L9/L15/L21 positions due to constitutional-anchor insertion). Single commit — light findings + 35-line file don't warrant Phase 41's three-commit cadence. CLAUDE.md grows 35→38 lines.
- Phase 42 audit-only pass on `CLAUDE.md` (per-install constitution doc) against the Phase 35b governance reframe — findings landed in `docs/scratch/claude-md-reframe-audit.md` flagging 1 high-impact finding (L3 dogfood tagline carrying older "orchestration layer" framing vs locked "structural immune system / governance layer"), 2 review-needed items (L5 "useful first, philosophy second" borderline-productivity framing; L25 un-translated "L0/L1/L2" jargon), and 2 coverage gaps (no constitutional-framework anchor in CLAUDE.md despite it being the per-install constitution; no "scope actively governed" anchor) across five criteria with refined (d) splitting placeholder-tuning-surface (positive signal) from locked-content tuning-context (gap check). Mirror parity to `template/CLAUDE.md.template` clean modulo six placeholders. Direction-only findings for chat-side triage; concrete amendment phase ships separately if warranted (Phase 41 precedent).
- Phase 41c aligned `CLAUDE_MANAGER.md` opening + § What this manager is + § Manager pattern with the Phase 35b governance reframe — opening adds executive-branch framing (constitutional principles in ROADMAP, watchdogs in audit triad), "ever-evolving being" reframed from skill-accretion to scope-discipline-evolution, and Manager pattern adds anchor that the shape is universal but content tunes per-project via `project-tuner-helper` + v1.2.0 manager-optimizer. Closes Findings #1, #2, #3 from Phase 40 audit.
- Phase 41b reframed `CLAUDE_MANAGER.md` § Template-content vs template-stubs map (template-as-seed framing replaces ships-as-is; per-project divergence is the default) and added three new principle-anchor H3s under § Strategic judgment patterns: Per-project governance, Flow + safety + both, and Guard rails — explicit override language ("You're now outside Claude Code's jurisdiction"). Also lined up L5 opening framing with the new layering. Mirror byte-identical to template modulo placeholder; closes Finding #5 + 3 coverage gaps from Phase 40 audit.
- Phase 41a corrected `CLAUDE_MANAGER.md` § How a plugin gets installed — replaced inaccurate "every install goes through `integration-checker`" claim with current-state language: `code-quality-auditor` is the v1.1.4 gate (SessionStart hook + 3 narrow heuristics), eyes-open install pattern handles trust-tier-2 plugins, `integration-checker` is planned for v2.0 per ROADMAP. Resolves the internal contradiction between L82–84 (DEFERRED) and L329–331 (in-effect) flagged in Phase 40 audit Finding #4.
- Phase 40 audit-only pass on `CLAUDE_MANAGER.md` against the Phase 35b governance reframe — findings landed in `docs/scratch/claude-manager-reframe-audit.md` flagging top sections needing amendment across five criteria (productivity-tool framing residue, one-size-fits-all assumptions, plain-English violations, missing per-project tuning context, principle conflicts). Direction-only findings for chat-side triage; concrete amendment phase ships separately if warranted.
- Phase 39 narrowed v1.2.0 gating bar from 3 target projects to 2 (TV + EoG) at `docs/ROADMAP.md` § v1.2.0 meta-evolution. Rationale: the per-project manager-optimizer's design needs structural variation, which TV (mobile-deployed Firebase) + EoG (Godot game) already cover; pinball stays as anticipated third sample, additive not gating. Multi-project graduation criteria unchanged — that mechanism's 3-project bar is load-bearing for cross-install pattern promotion, separate from v1.2.0 design start.
- Committed Phase 36 retire/repurpose framework draft to `docs/scratch/phase-36-framework.md` as durable reference for Phase 36 execution (post-Phase-35-close). Working-draft status; six H2s cover trigger conditions, evaluation criteria (dispatch frequency / ecosystem overlap / structural role / per-project divergence), decision outcomes (keep / retire / repurpose / replace), and pre-evaluation of `plan-coordinator` as retire-candidate pending Phase 35 empirical data. Scratch placement intentional — graduates to canonical location when Phase 36's first real execution surfaces what's underspecified.
- Phase 38 v3.0+ multi-LLM motivation note: amended `docs/ROADMAP.md` § v3.0+ multi-LLM sibling project with three additive paragraphs covering (a) execution/judgment split as cost-saving motivation — bulk execution dispatches to cheap LLMs (MiniMax / Kimi-K2 / Gemini Flash / DeepSeek / GLM / Qwen-Coder as examples), Claude handles planning/review/judgment; manager's arbitration role becomes "which model handles which subtask"; (b) Reddit-pattern reference architecture — lock-file coordination + tmux grid for parallel multi-agent dispatch is the architectural seed, not a spec; bridge will design its own concrete architecture when the project opens; (c) scaling caveat — at 1-person × small-N projects review is cheap and savings compound; at multi-person × multi-project review becomes the bottleneck. Governance discipline (approval-gated autonomy, batched audit cadence, per-project tuning) transfers as the skeleton-to-bridge lineage; bridge needs its own answer to review-at-scale. Existing locked decisions preserved intact (sibling project not feature graft, out of scope through v2.0, working title `claude-skeleton-bridge`). Doc-only — no behavior change.
- Phase 37 strategic-audit-cycle codification: added new `## Strategic audit cycle` H2 to `CLAUDE_MANAGER.md` + `template/CLAUDE_MANAGER.md.template` (byte-identical modulo `{{PROJECT_NAME}}` placeholder) with three H3 subsections — when the cycle runs (event-triggered: post-major-ship, post-empirical-event, before-release-cut — NOT calendar-scheduled), two-tier audit pattern (strategist chat tier for sequencing/contradictions/principle drift + CC tier for line-by-line/schema/code-vs-doc; combined output exceeds either alone, Phase 30 → 34b is the canonical empirical case closing a 9-phase emergent queue over 2 days), and audit-emergent queue handling (mechanical findings fold into next release as additive commits; larger findings queue as named phases per the 30b/c pattern; strategic findings codify via Model C into the directive layer that architecturally fits). Composes with — does NOT replace — v1.2.0's scheduled `infrastructure-auditor` (project-level) and `roadmap-auditor` (skeleton-level); those are scheduled audits against in-project surfaces, this is the chat-side + CC-side cycle against the directive layer and roadmap. Extends Model C precedent (Phase 17 plan-amendment, Phase 18 lesson surface, Phase 35a plain-English). Doc-only — no behavior change; no new agents / scripts / hooks / schemas.
- Governance reframe: rewrote `docs/STORY.md` + `docs/ROADMAP.md` to lock claude-skeleton's identity as a structural immune system / governance layer for Claude Code projects. STORY.md opens with the scope-decay + AI-generated-slop problem and lays out 8 core principles (approval-gated autonomy, per-project governance, multi-project graduation, flow + safety + both, compose-don't-compete, guard rails configurable but transparent, quality filter not directory, plain English by default), the prevention-pipeline walkthrough, and the audience/non-audience clarification. ROADMAP.md reorders Sequencing as ordinal + gate-based (NO calendar dates, NO day-by-day granularity — empirical readiness is the gate) and reframes v1.2.0 around a **per-project manager-optimizer** (one instance per installed project, each watching its own context — "Pinball governs pinball. Trainer-View governs TV.") instead of single-instance dogfood watching. New architectural principles added (additive only, existing 12 carry over byte-verbatim): per-project governance, flow + safety + both, guard rails configurable but transparent, scope actively governed not passively hoped. Three new cuts logged: single-manager-optimizer (dogfood-only watching), one-size-fits-all install, "AI productivity tool" framing. Both files include verbatim anchor phrases from the strategist session ("structural immune system", "AI-generated slop", "scope is actively governed, not passively hoped for", "approval gate gets smarter, not more annoying", "You're now outside Claude Code's jurisdiction", "Don't be a directory; be a quality filter"). Doc-only — no behavior change; v1.1.4 substrate unchanged.
- Phase 52 raw-template baseline + inline one-time migration: switched `scripts/update.sh` classification from the mutable `files` map to a new immutable `raw_template_baselines` marker field (the template version each file was installed from). Fixes the regression where `project-tuner-helper` customizations were classified `TEMPLATE_UPDATED` and silently overwritten on `[A]pply all` — they now correctly surface as `LOCALLY_MODIFIED` (never auto-applied, even under `--auto-apply`), whoever changed them. Root cause was `files` being mutated post-install (the backfill path set recorded=current, baking tuner content in as the baseline); the prior "install records the baseline after the tuner" framing was inaccurate — `install.sh` records during `cp`, before the tuner, and never invokes it (`integration-installer` dispatches the tuner afterward and never touches the marker). `install.sh` now writes `raw_template_baselines` alongside `files` (byte-identical at install); `files` is retained as a deprecated back-compat alias (removal queued v1.5+). Pre-Phase-52 markers (have `files`, no `raw_template_baselines`) migrate once, inline, on the next `update.sh` run by `git worktree`-pinning the recorded install commit and re-hashing the template there — gated so legacy shell markers (no `files` object) keep using the existing 0.8.0 backfill path; never hard-fails (an unresolvable commit degrades to baseline=current-template, which still surfaces tuned files as `LOCALLY_MODIFIED`); `raw_template_baselines` is written only when non-empty so `--check-remote` cannot pre-empt migration with an empty object. Verified against the live dogfood marker (commit `bba2d8a`): recovered 45/49 baselines, 4 safe-fallback, `settings.json` drift correctly `LOCALLY_MODIFIED`, no marker write under `--dry-run`, no stray worktree. Two new CI scenarios (`raw-baseline-install`, `raw-baseline-migrate`) + `ci.yml` steps land with their schema changes (Phase 46c co-ship rule); all 13 scenarios green. Docs: new `## Baseline semantics under update.sh` H2 in `CLAUDE_MANAGER.md` + `template/CLAUDE_MANAGER.md.template` mirror; `docs/INSTALLATION.md` marker-schema section updated. No VERSION bump — [Unreleased].

- Between-phase governance commits, accounted at the cut (their briefs carried no CHANGELOG deliverable): the INSTALLATION share-mode truth fix (`a373487`), the first task-watchdog triage dispositions (`9360a67`), the edit-before-read capture shipped as the compactPrompt durable rule (`1571cbb`), and the propagation spec's approve/consume lifecycle chores (`b878a66`, `d56c781`).

## [1.1.4] - 2026-05-17 — Plugin-verification surface open

v1.1.4 opens the plugin-verification surface with `code-quality-auditor` (Phase 24) — first concrete plugin-audit component, composing with `cruft-checker` + `drift-checker` as the **project-level audit triad** that fires at SessionStart. Three narrow heuristics ship: manifest-honesty (i + ii) and security-hygiene (iii — destructive shell patterns against unguarded paths). Semantic "fitness vs description" checks (Layer 3 originally scoped) remain deferred to v2.0 alongside `integration-checker` (Layer 1+2). The phase piggybacked a destructive-pattern shared-lib extraction — the Phase 14c / Phase 21 PreToolUse hooks now source their pattern arrays from `.claude/lib/`, so real-time blocking and retrospective audit operate against a single source of truth.

### v1.1.4

- `code-quality-auditor` ✓ *(shipped — Phase 24)* — first concrete plugin-verification component. Ships 3 narrow heuristics: (i) manifest-declared component path missing or empty, (ii) `hooks/` present but `hooks.json` malformed or empty (reuses Phase 16 viii hook-schema validation), (iii) destructive shell patterns in plugin scripts against unguarded paths (reuses bash-safety + powershell-safety pattern sets via shared `.claude/lib/` extraction). Routes via `workflow-suggester` as `suggested_artifact_type: manual_action`; capture filename convention deviates to `.claude/captures/plugin-quality-<plugin>-<heuristic>.md` (idempotency preserved via frontmatter `source_pattern_id`). Composes with `cruft-checker` + `drift-checker` as the **project-level audit triad**. Ships in `template/` (target projects also install plugins). 24h cooldown via `.claude/.last-plugin-quality-check`; `--plugin-dir` flag for testing. Semantic "fitness vs description" checks deferred to v2.0 alongside `integration-checker` (Layer 1+2).
- Destructive-pattern shared lib extraction — moved `DESTRUCTIVE_BASH_PATTERNS` / `DESTRUCTIVE_POWERSHELL_PATTERNS` from inline declarations in the two PreToolUse safety hooks (Phase 14c / Phase 21) into shared `.claude/lib/destructive-{bash,powershell}-patterns.sh`. Both hooks AND the new `plugin-quality-check.sh` heuristic iii source the libs — single source of truth across real-time blocking and retrospective audit. Hooks fail-closed if lib missing. Phase 21 hooks' behavior unchanged post-refactor (verified by regression-testing same inputs).
- Schema extensions: `session-observer.schema.md` `source` enum gains `cruft-checker` (v1.1.x backfill — was implicitly active but undocumented in the enum) and `code-quality-auditor` (v1.1.4); `pattern_type` enum gains `plugin_quality`. `workflow-suggester.schema.md` documents the new routing rule and filename-convention deviation. No new `suggested_artifact_type` enum value — reuses the existing baseline `manual_action`.
- Settings.json: SessionStart hook chain extended with `plugin-quality-check.sh --hook` entry (3rd in dogfood after `sessionstart-rules.sh` + `cruft-check.sh --hook`; 2nd in template after rules). Mirror invariant honored — intentional drift list unchanged (defaultMode, dogfood-only cruft-check hook, compactPrompt placeholder).
- CI scenarios.sh `verify_marker 41` → `45` for 4 new template files (agent doc + script + 2 lib files).

## [1.1.3] - 2026-05-17 — Operational-friction relief

v1.1.3 closes operational friction across the v1.1.x polish arc: Phase 20 ends the heuristic-x FP storm on legitimate historical version annotations; Phase 21 closes the Windows tool-ID gap with PowerShell bare allow + safety-hook parity that was inflating `settings.local.json`. No new components, no schema changes — both items are operational tightening on existing primitives. Closes the v1.1.x polish arc that started post-v1.1.0 (capture/reuse loop) and ran through v1.1.1 (hook infrastructure correctness) and v1.1.2 (captures-surface enum completion + lesson codification flow).

### v1.1.x

- cruft-checker heuristic-x scope tuning — extended `EXEMPT_VFILES` and added `EXEMPT_VDIRS` to cover `.claude/agents/05_meta/` + `template/.claude/agents/05_meta/` (agent and schema docs), `CLAUDE_MANAGER.md`, and `template/.claude/captures/README.md`; added `<!-- cruft-check:exempt-historical -->` inline marker convention (same-line OR preceding-line, marker on its own line exempts the immediately following line) and applied it to README / `claude-skeleton-handoff.md` / `docs/ROADMAP.md` stragglers outside V-heading regions. Drops ~55 FPs against legitimate historical version annotations without losing catch on real stale forward refs. Markdown-comment style only this phase; other comment shapes deferred. cruft-checker agent doc updated: heuristic-list count corrected (seven → nine), heuristic 8 / viii moved off the deferred line (now reflects v1.1.2 ship), new "Inline exemption marker" section documents the convention with usage examples. Dogfood-only — no template mirror, per the locked principle.
- settings.json bare-tool allows broadened — added `PowerShell` to cover the Windows tool-ID gap (PowerShell is a separate `tool_name` from Bash; the bare `Bash` allow doesn't cover it). New `pretooluse-powershell-safety.sh` hook mirrors the bash-safety shape with PowerShell-specific destructive patterns: `Remove-Item -Recurse -Force` (canonical + aliases `ri` / `rm` / `del` / `erase`, short flags `-r` / `-f`, either order), `Format-Volume`, `Clear-Disk`, `Set-ExecutionPolicy Unrestricted` / `Bypass`, `iwr` | `iex` and `Invoke-WebRequest` | `Invoke-Expression` pipe-to-shell, force-push / hard-reset-to-origin (same syntax in PowerShell as bash). Case-insensitive via `shopt -s nocasematch`. Same locked default-allow design: deny destructive patterns, allow everything else. PreToolUse settings gain a second matcher block scoped to `PowerShell`. Dogfood + `template/.claude/hooks/` byte-identical mirror; settings mirror invariant honored (intentional drift on `defaultMode`, dogfood-only cruft-check SessionStart hook, `compactPrompt` placeholder vs resolved). CI `verify_marker 40` → `41` for the new template file. Drops recurring "Always allow" prompts on routine PowerShell work without loosening the destructive deny surface. bash-safety unchanged — empirical audit (direct invocation of the hook with non-destructive Bash commands) confirmed pre-existing default-allow on unmatched non-destructive patterns already matched the Phase 14 locked design; the brief's diagnosis-(2) framing didn't apply.
- README hook count corrected from 4 to 5 (Phase 21 added `pretooluse-powershell-safety.sh` to `template/.claude/hooks/`; the "Baseline tooling" bullet on the README hadn't been bumped at the time). Surfaced by the v1.1.3 pre-cut cruft-check sweep — heuristic iv. Folded into the release-cut commit per the Phase 6 "sweep known doc-rot at release cuts" rule.

## [1.1.2] - 2026-05-16 — Captures-surface enum completion + hook-schema validation

v1.1.2 completes the captures-surface `suggested_artifact_type` enum expansion for v1.1.x — `script`, `skill`, `agent`, `command`, `manual_action`, `unclear` baseline plus v1.1.x's `doc-fix`, `infrastructure-fix`, and `lesson`. Phase 16 closes the hook-schema failure mode that took the 14c→14f arc five phases to surface — now caught at first cruft-check. Phase 17 codifies plan-amendment behavior as a `CLAUDE_MANAGER.md` directive subsection, demonstrating the lesson-codification flow that Phase 18 then formalizes as a first-class capture type.

### v1.1.x

- Added `lesson` to `workflow-suggester`'s `suggested_artifact_type` enum (v1.1.2 surface-only landing — completes the captures-surface enum expansion for v1.1.x: `script`, `skill`, `agent`, `command`, `manual_action`, `unclear` baseline plus v1.1.x's `doc-fix`, `infrastructure-fix`, and `lesson`). Routing extended: notes starting `"lesson: "` route to `suggested_artifact_type: lesson`; user-authored captures may set the field directly. **No producer heuristics ship in this phase** — autonomous detection of lesson-shaped observations is deferred until grounding data exists, same discipline as task-watchdog's deferred resource-anomaly signals. **No X-builder ships** — lessons codify manually via small-fix commit into the directive surface that architecturally fits (`CLAUDE_MANAGER.md`, `docs/ROADMAP.md`, session-handoff doc, etc.); Phase 17's plan-amendment-behavior codification into a new `CLAUDE_MANAGER.md` H3 is the canonical precedent. Capture frontmatter's `shipped_to:` field records the codified location.
- Codified plan-amendment behavior in CLAUDE_MANAGER directive layer (new `### Plan amendment behavior` subsection under `## Strategic judgment patterns`). The failure mode the rule prevents: Claude Code's default tendency to re-plan on any new input — even small clarifications — which loses prior plan structure and forces full re-review. The codified behavior: apply the amendment as a delta by editing the existing plan file in place, do not regenerate the plan from scratch. Honors the explicit user phrasing `DO NOT re-plan or rewrite — apply the delta and proceed` that emerged as a recurring v1.1.x mitigation (notably during Phases 14f and 16). Phase 9 mirror invariant honored — template + dogfood copies edited in lockstep.
- Added cruft-checker heuristic viii (hook-entry config-schema validation). Walks every `hooks.<EventName>[*].hooks[*]` entry in `.claude/settings.json` and `template/.claude/settings.json.template`, flags missing `type: "command"` or missing `command` field — the v1.0 → Phase 14c-diag silent-inert failure mode that took five phases (14c → 14c-diag → 14d → 14e → 14f) to surface. Paired with new `docs/HOOK_SCHEMA.md` reference doc (cites Anthropic canonical) and `infrastructure-fix` value added to `workflow-suggester`'s `suggested_artifact_type` enum (config-fix captures, distinct from `doc-fix`'s prose/markdown shape). Closes the v1.1.x hook infrastructure arc retrospectively: failures of this class get caught at the first cruft-check next time, not after operational verification. Hook script *output*-schema validation (the Phase 14e `hookSpecificOutput` wrapper) explicitly remains out of scope — a different check kind, deferred to a future heuristic if needed.

## [1.1.1] - 2026-05-16 — Hook infrastructure correctness

v1.1.1 makes the skeleton's hook infrastructure actually work for the first time since v1.0. PreCompact, SessionStart (rules + cruft-check), SessionEnd, and the new PreToolUse hooks now all fire as designed — previously each was missing the required `type: command` field on its inner hook object, and CC's schema validator silently dropped them. Also ships permissions hardening (canonical bare-tool-name allow patterns + PreToolUse hook for destructive-command blocking), the observation resolution mechanism (producers mark observations resolved on re-scan), the dogfood directive layer at skeleton repo root, commit-cadence-by-phase-size rubric, and the v1.1.0 polish that prompted the tier.

### v1.1.x

- Added `cruft-checker` (v1.1.x first component, third observation producer against `session-observer`'s 8-field schema after `session-observer` and `task-watchdog`). **Dogfood-only** — lives in skeleton's own `.claude/agents/05_meta/cruft-checker.md` + `.claude/scripts/cruft-check.sh`, NOT in `template/`. Per the locked two-audit-surfaces principle: skeleton-level cruft stays in dogfood; project-level cruft is v1.2.0's `infrastructure-auditor` (template-resident). v1.1.x ships 7 heuristics: (i) markdown link to missing file; (ii) markdown link to missing header (with slugify); (iii) `VERSION` ↔ top-dated `CHANGELOG` header mismatch; (iv) README baseline-tooling count claims vs `template/.claude/` actuals (agents / skills / scripts / commands / hooks); (v) `Phase N` references not in CHANGELOG / handoff / git history; (vii) stale `N-field schema` claims vs actual schema field count; (ix) tag ↔ VERSION ↔ top CHANGELOG header consistency at HEAD when tagged; (x) stale cross-doc `vX.Y.Z` references (< current VERSION) outside exempt files (CHANGELOG / SESSION_LOG / INSTALLATION / ARCHITECTURE / STORY) and exempt regions (level-2 or level-3 v-prefixed headings in ROADMAP / handoff). Heuristic 6 (deprecation-pattern references) and heuristic 8 deferred to follow-up phase. All emissions use `pattern_type: other` with descriptive notes; `pattern_id = sha256("other" + per-violation-signature)` keeps observations stable across runs (existing-file merge bumps occurrences). 24h cooldown gated by `--hook` flag at `.claude/.last-cruft-check`; direct dispatch (no flag) ignores cooldown. SessionStart hook wired as a second entry in dogfood `settings.json` (sessionstart-rules.sh stays byte-identical with template). Schema extension to `workflow-suggester`: `suggested_artifact_type` enum gains `doc-fix` (additive, existing values preserved); `cruft-checker` observations flow through `workflow-suggester` and draft captures with that type — manager applies fixes manually since no `doc-fix-builder` X-builder ships in v1.1.x. CLAUDE_MANAGER directive layer extended with `### When to dispatch cruft-checker` subsection (template only — no dogfood twin); helper roster table unchanged at 11 rows (cruft-checker is dogfood-only). `.gitignore` updated to exclude runtime observation files + cooldown markers across all three producers.
- Fixed workflow-suggester field-count doc-rot (7→8 fields, Phase 3c carry-over). Surfaced by cruft-checker smoke test in Phase 7.
- v1.1.x doc-rot sweep — corrected cross-doc field-count refs in ROADMAP and handoff, genericized drift-checker illustrative example, mirrored bash-safety skill into dogfood (closed one-skill mirror gap).
- Established dogfood directive layer at skeleton repo root (`CLAUDE.md` + `CLAUDE_MANAGER.md` + `ROUTING.md`), making the skeleton its own first installed project. Files resolved from `template/*.template` with placeholders filled for skeleton-as-project values (`{{PROJECT_NAME}}` → `claude-skeleton`, `{{DEPLOY_COMMAND}}` → `N/A — skeleton has no deploy step`, etc.); all other content byte-identical to templates. Trimmed `compactPrompt` in `.claude/settings.json` from 6 rules to 5 — removed `Scripts for mechanical work...` since it duplicates the `## Dispatch mechanics` section of `CLAUDE_MANAGER.md`; compactPrompt's purpose is now purely operational re-anchor after auto-compact, strategic content canonicalizes in `CLAUDE_MANAGER.md`. New `## Dogfood mirror invariants` section in both template and dogfood `CLAUDE_MANAGER` locks the rule: template-root `.template` files have byte-identical dogfood mirrors at skeleton repo root, differing only in resolved placeholder values; phase-brief-scoped dogfood-only artifacts (`cruft-checker`) are explicit exemptions, not gaps.
- Added commit-cadence-by-phase-size rubric to CLAUDE_MANAGER directive layer (small fix / medium phase / large overhaul). Eliminates need for prompt-side COMMITS overrides on small work going forward.
- Extended commit-cadence rubric: every shipped phase ships a [Unreleased] CHANGELOG bullet by default. Closes the gap that caused Phase 8/8b/10/11 to require backfill commits.
- Added CC permissions allow-list to settings.json (6 allow patterns, 10 deny patterns targeting destructive shapes). Eliminates per-tool-use prompts for common operations; preserves prompts on rm -rf, force push, hard reset, chmod 777, pipe-to-bash patterns.
- Extended observation schema with `resolved_at` field. Producers (session-observer, task-watchdog, cruft-checker) now mark observations as resolved when underlying patterns are no longer present. Workflow-suggester filters resolved observations from capture generation. Migrated existing observations; closed out 5 lingering observations from Phase 8b cruft fixes.
- cruft-checker tuning: heuristic vii now exempts `docs/CHANGELOG.md` (historical entries are accurate-at-ship-time, same logic as heuristic x). V_HEADING_RE extended to also match Phase-recap heading shapes (`### Phase N — ... vX.Y.Z`), closing the handoff:60 Phase-recap FP and the CHANGELOG:26 historical schema-claim FP. handoff:136 stays active — its `### Sprint rules locked in v1.1+` heading isn't a Phase-recap shape; deferred to follow-up if a different exemption is wanted.
- CC permissions pattern fix: corrected allow/deny syntax against canonical Anthropic docs (https://code.claude.com/docs/en/iam). Phase 11's `Edit(*)`/`Write(*)`/`Read(*)` used gitignore semantics where `*` is single-directory only — bare tool names (`Edit`, `Write`, `Read`) are the canonical "match all" form. Phase 11's 4 pipe-to-shell deny rules (`Bash(curl * | bash)` etc.) were silent no-ops: `|` is a subcommand separator, so patterns containing `|` can never match a single subcommand. Dropped the 4 broken rules and documented the gap (pipe-to-shell hardening deferred to future hook-based phase). Broader allow scope added: `WebFetch`, `WebSearch`, `NotebookEdit`, `Agent`. Final shape: 10 allow patterns, 6 deny patterns (Phase 11's 6 working denies retained). Net effect: future sessions should not prompt for normal operations.
- Replaced fragile string-pattern denies with PreToolUse hook (`pretooluse-bash-safety.sh`) per canonical Anthropic docs recommendation: "Bash permission patterns that try to constrain command arguments are fragile." Hook intercepts Bash invocations BEFORE subcommand splitting, blocks 10 destructive patterns including the 4 pipe-to-shell shapes Phase 11's deny rules couldn't catch (`|` is a subcommand separator in the permissions schema). Phase 14 verified deny rules don't reliably fire even when patterns SHOULD match; this phase replaces that defense surface entirely. Removed 6 fragile deny patterns from `settings.json` (deny key removed entirely). Pruned 85 redundant entries from `settings.local.json` (gitignored, dogfood-only operational cleanup).
- Phase 14c-diag surfaced that ALL hook entries in settings.json have been missing the required `type: command` field on the inner hook object since v1.0 — CC's schema validator silently dropped invalid hook entries while still applying the surrounding permissions block. Affected hooks: PreCompact backup (since v1.0), SessionStart rules injection (since v1.0), SessionStart cruft-check (Phase 7), SessionEnd session-observer (since v1.0), PreToolUse bash-safety (Phase 14c). All have been silently inert. Phase 14d adds `type: command` to every hook entry. Affected agents and scripts are unchanged — they work fine on manual dispatch; what was broken is automatic triggering, which now actually fires. Discovered when Phase 14c's PreToolUse hook had visible failure consequences (test 6b ran a destructive curl|bash that should have been blocked); existing hooks failed silently because their absence didn't break anything visible. Additionally surfaced during Phase 14d verification: `sessionstart-rules.sh` emitted bare `{additionalContext: ...}` instead of the canonical `{hookSpecificOutput: {hookEventName: "SessionStart", additionalContext: ...}}` wrapper, so CC silently dropped its output even after the hook started firing. `cruft-check.sh --hook` escaped the same bug because it emits no stdout (side-effect only — writes marker + observation files). Fix bundled into this phase. Pattern lesson: any future SessionStart hook injecting `additionalContext` must use the wrapped schema; side-effect-only hooks are immune.
- Gitignored hook runtime artifacts (`docs/SESSION_LOG.md` appends, `.claude/observations/.session-ended` marker). Phase 14d+14e made hooks fire for the first time since v1.0; they correctly produce per-session runtime state, which doesn't belong in repo history. Real release narrative lives in CHANGELOG. Same architectural pattern as gitignored `.claude/observations/*.json` and `.claude/.last-cruft-check`. Also fixed stale dogfood `.gitignore` line covering wrong path (`.claude/.session-ended` → `.claude/observations/.session-ended`) and mirrored the two new patterns into `template/.gitignore.template` per the dogfood-mirror invariant so target projects (TV, EoG, Fitness-Website) inherit the same posture on next `update.sh`. Untracked the existing `docs/SESSION_LOG.md` via `git rm --cached` — local copy retained; session-observer continues reading the local file; fresh installs get a SESSION_LOG via `template/docs/SESSION_LOG.md.template`.

## [1.1.0] - 2026-05-15 — The capture/reuse loop

The capture/reuse loop. Five components closing autonomy Gap #2 (system-proposes-own-evolution): `session-observer` notices recurring patterns from `SESSION_LOG.md` at session end, `workflow-suggester` drafts capture files from those observations into `.claude/captures/`, `script-builder` formalises approved captures into bash drafts under `.claude/scripts/drafts/`, `drift-checker` surfaces version drift between the installed skeleton and the latest released tag at session start, `task-watchdog` reads the prior session's JSONL transcript to detect long-running bash calls and recurring failures. Pipeline: observations → captures → drafts → promoted artefacts, with user-approval gates between each stage. Plus `opusplan` default (Opus plan-mode + Sonnet execution) and additive `.skeleton-version` schema extensions (`cached_skeleton_head` + `cached_skeleton_head_fetched_at`) to support the new flow. Used in production daily on Trainer-View and Echoes-Of-Gill.

- Default model set to `opusplan` in `template/.claude/settings.json.template` (Opus plan-mode + Sonnet execution). Skeleton's own dogfood `.claude/settings.json` updated to match. Override via `.claude/settings.json` per project.
- Added `session-observer` foundation (v1.1+ capture/reuse loop Phase 1): observation primitive at `template/.claude/agents/05_meta/session-observer.md`, contract schema at `session-observer.schema.md` (8 fields, multi-source-extensible enums for `source` and `pattern_type`), storage surface at `template/.claude/observations/` (one JSON file per `pattern_id`), and SessionEnd hook script at `template/.claude/hooks/sessionend-observe.sh` that records the session boundary. Observation only — no suggesting, drafting, or auto-action; those land in Phase 2 (`workflow-suggester` extension).
- Added `workflow-suggester` v1.1+ contract (v1.1+ capture/reuse loop Phase 2): consumes `.claude/observations/`, drafts capture markdown to `.claude/captures/` for human review, idempotent across re-runs (any capture status — `draft` / `approved` / `rejected` — counts as "already considered"). Default thresholds: `occurrences >= 3 AND confidence >= medium`, tunable via direct edit. Capture schema at `template/.claude/agents/05_meta/workflow-suggester.schema.md` defines a 7-field YAML frontmatter + 4-section body convention. Wholesale rewrite of the v1.0 prose-suggestion agent: input surface is now observations (not `SESSION_LOG.md` directly), output is markdown files (not prose return values), gains `Write` tool, drops `model: sonnet` override (inherits `opusplan`). Pure drafting — does not build downstream artifacts (script-builder lands next), does not modify observations, does not auto-approve.
- Added `script-builder` (v1.1+ capture/reuse loop Phase 3, first X-builder): consumes `.claude/captures/*.md` filtered to `status: approved AND suggested_artifact_type: script`, drafts bash scripts under `.claude/scripts/drafts/<source_pattern_id>.sh.draft` following the 5-section discipline (shebang+strict-mode / constants / helpers / main / cleanup) + path-shape guards + `bash-safety` conventions baked in, with the `.sh.draft` extension as a "don't run me yet" marker. Pattern source: `commit.sh`, `deploy.sh`, `install.sh`. Draft-first: user reviews, optionally edits, then renames to `<descriptive-name>.sh` + `chmod +x` + flips capture status `approved` → `shipped` with `shipped_to:` recorded. Idempotent re-runs by filename match. Does NOT auto-promote, modify captures, handle non-script artifact types, or auto-dispatch on capture status flip (manual dispatch only in v1.1.0; Gap #1 not closed). Additive lifecycle extension to `workflow-suggester.schema.md`: status enum gains `shipped` (terminal success state, user-set after promote) and new optional `shipped_to` field (path of promoted artifact). Existing `draft` / `approved` / `rejected` semantics unchanged.
- Added `drift-checker` (v1.1+ capture/reuse loop Phase 4, fourth and smallest v1.1.0 component): read-only version-drift check at session start. Agent at `template/.claude/agents/05_meta/drift-checker.md` plus 5-section bash mechanic at `template/.claude/scripts/drift-check.sh` (~75 lines). Compares the installed `version` field in `.claude/.skeleton-version` against the cached `cached_skeleton_head` field; emits a `[skeleton-drift]` notice block when they differ, when the cache is empty/null, or when the marker is missing/malformed; silent when in sync. Always exits 0 — failure never blocks the SessionStart hook chain. Schema extension to `.skeleton-version` (additive): two new optional fields `cached_skeleton_head` (string | null) and `cached_skeleton_head_fetched_at` (ISO timestamp | null); both written by `install.sh` as `null` on fresh install and by `update.sh` migration path as `null` on backfill. New `update.sh --check-remote` flag is the only network path: runs `timeout 10 git ls-remote --tags` against the skeleton repo, picks the highest semver tag, writes the marker; no diff/classification flow runs. SessionStart hook (`sessionstart-rules.sh`) extended to invoke `drift-check.sh` and fold the notice into `additionalContext`; matcher dropped from settings.json so the hook fires on every SessionStart event (not only after auto-compaction). Locked invariants: drift-checker NEVER hits the network, NEVER writes the marker, NEVER auto-applies updates; file-level drift detection stays out of scope (that's `update.sh`'s six-way classification). CI: file count 35 → 37 in `.github/test-fixtures/scenarios.sh` (one new agent + one new script in `template/`). Dogfood mirror byte-identical for shared artefacts. CLAUDE_MANAGER directive layer extended with `### When to dispatch drift-checker` subsection + helper roster row 10.
- Added `task-watchdog` (v1.1+ capture/reuse loop Phase 5, fifth and final v1.1.0 component): retrospective observer of the prior Claude Code session. Agent at `template/.claude/agents/05_meta/task-watchdog.md` (~100 lines) + 5-section bash + inline-python mechanic at `template/.claude/scripts/task-watchdog.sh` (~230 lines). Reads `~/.claude/projects/<encoded-cwd>/<session-id>.jsonl` (the second-newest JSONL by mtime — newest is the in-progress current session), pairs `tool_use`/`tool_result` events by `tool_use_id`, filters out subagent transcripts (`isSidechain == true`), and emits two pattern shapes into `.claude/observations/` using session-observer's existing 8-field schema: (1) long-running bash calls where `durationMs >= 300000` (5min default, exposed as constant) → `pattern_type: other` with `notes: "long-running bash call (>Nm)"`, allowing `occurrences=1` as an anomaly signal; (2) recurring failures where the same normalized error signature occurs ≥3 times in-session → `pattern_type: recurring_failure`. SHA-256 pattern_ids over `pattern_type + normalized_signature` keep observations stable across re-runs and cross-session accumulation. Idempotency via `.claude/observations/.last-watchdog-session` marker holding the most recently processed sessionId. Path encoding mirrors observed Claude Code format (strip drive colon, replace path separators with `-`); fallback scans `~/.claude/projects/*` and matches by `cwd` field if encoded path doesn't resolve. Always exits 0 — failure never blocks the SessionStart hook chain. Hook wiring: `sessionstart-rules.sh` extended from two jobs (rules + drift) to three (rules + drift + watchdog); all three pieces fold into a single `additionalContext` block. Locked invariants: NEVER polls live (retrospective only — real-time signals defer until Claude Code surfaces a polling hook), NEVER hits the network, NEVER writes outside `.claude/observations/`. Resource-anomaly signals (memory, tokens, CPU) are explicitly deferred to v1.2.0's `token-efficiency-monitor` proactive upgrade. v1.1.0 file count 37 → 39. Dogfood mirror byte-identical. CLAUDE_MANAGER directive layer extended with `### When to dispatch task-watchdog` subsection + helper roster row 11. Marks v1.1.0 feature-complete; release-cut + version tag is a separate phase.
- **session-observer recurring-failures ownership transfer** (Phase 5): `session-observer` no longer emits `recurring_failure` observations — `task-watchdog` is now the canonical producer of that pattern type. Coordinated change in the same phase to avoid double-emission. session-observer's frontmatter description, body bullet for `recurring_failure`, and confidence-heuristic line all updated to reflect the transfer; the agent now lists "commands run, files edited, error-resolution sequences" as its signal scope (no longer "failures recurring"). `session-observer.schema.md` producers list split into per-pattern ownership: "session-observer for all pattern types except recurring_failure; task-watchdog for recurring_failure + other-typed long-running observations." Pattern_type table row for `recurring_failure` annotated with "(canonical producer: task-watchdog)." Schema field set unchanged — no new fields, no new enum values. Existing `recurring_failure` observations in any installed project continue to be valid; future ones come from task-watchdog only.

## [1.0.0] - 2026-05-14 — Orchestration layer ready for daily use

The first stable cut. Install / update infrastructure shipped, directive layer locked, validated on two real production targets. v1.0 is the orchestration layer ready for daily use across multiple real projects; v1.1+ work (the capture/reuse loop) starts on top of this baseline.

**Strategic layer.** `template/CLAUDE_MANAGER.md.template` is the directive layer the manager reads at session start. Carries the strategic judgment patterns (when to dispatch a helper vs read directly, when to escalate vs propose, when to question framing vs execute), the recursive ownership L0 / L1 / L2 framing (L3 reserved for v1.2+ `manager-optimizer`), the three-commit cadence (work / docs / VERSION+CHANGELOG → push), and the plugin marketplace composition section naming the seven ecosystem sources claude-skeleton composes with. Verbatim design principle: *"Don't be a directory; be a quality filter."*

**Infrastructure.** `scripts/install.sh` (three modes: fresh / merge / replace; atomic JSON marker write; rollback on any error). `scripts/update.sh` (six-way classification using per-file SHA-256 hashes recorded at install time, with one-time backfill for legacy shell-format markers). `template/.claude/scripts/commit.sh` (verbatim five-section commit wrapper) and `template/.claude/scripts/deploy.sh` (uncommitted-changes check, POST-DEPLOY SMOKE TEST banner). GitHub Actions CI on Ubuntu / macOS / Windows runs six install/update scenarios on every push and every PR (`fresh-install`, `fresh-refuse`, `merge-add`, `local-mod-detect`, `local-mod-preserve`, `backfill-migrate`).

**Agents, skills, commands.** Nine baseline agents: `research-helper`, `audit-helper`, `monitoring-helper`, `plan-coordinator`, plus the five 05_meta agents — `project-tuner-helper`, `system-memory-helper`, `agent-slicer`, `workflow-suggester`, `self-audit-helper`, `integration-installer`. Six baseline skills: `schema-verify-before-edit`, `post-edit-test-suggest`, `god-file-grep-first`, `bash-safety` (noise-path excludes, timeout discipline, background-bash wait/kill rules — prevents zombie tasks from unbounded scans hitting `.git`/`.godot`/`node_modules`/build caches), `token-efficiency-monitor`, `plugin-roster-search`. Four slash commands (`/commit`, `/audit`, `/deploy`, `/smoke-test`). Two hooks (`sessionstart-rules.sh`, `precompact-backup.sh`).

**Validation.** Retrofitted onto and stable across two real production targets at different stack profiles: **Trainer-View** (Flutter + Firebase, mobile app shipping features) and **Echoes-Of-Gill** (Godot, game in active development). Two stacks, two team sizes, two update cadences — install/update path survives both.

**Sequencing.** `docs/ROADMAP.md` orders v1.1+ / v1.2+ / v2.0. The v1.1+ centerpiece is the **capture / reuse loop**: `session-observer` notices recurring patterns in actual session work, `workflow-suggester` evolves from suggesting in the abstract to drafting concrete proposed-helper / proposed-script files, `/goals` provides the clarifying-questions layer, `script-builder` formalises approved captures into reusable scripts following the 5-section discipline. Closes the four named autonomy gaps in one named loop.

**Story.** `docs/STORY.md` is the canonical narrative — what claude-skeleton is, why it exists (ADHD-driven design rationale as constraint-forced discipline that turns out to be generally useful for LLM collaboration over weeks), how it works, what's distinctive, and what it isn't. 1839 words, peer-conversational register.

## [0.9.0] - 2026-05-14 — CI: automated three-platform validation

- **`.github/workflows/ci.yml` — three-platform matrix CI.** Runs on every push to `main` and every PR across `ubuntu-latest`, `windows-latest`, and `macos-latest`. `fail-fast: false`, so a single-platform break still surfaces results from the other two. Uses `actions/checkout@v4` and `actions/setup-python@v5` (3.11), then a sequence of named scenario steps from `.github/test-fixtures/scenarios.sh`.
- **`.github/test-fixtures/scenarios.sh` — six scenarios.** Each runs in its own `mktemp -d` target, runs assertions, and trap-cleans:
  - `fresh-install` — clean target → marker is valid JSON with 25 files, all 64-char hex hashes.
  - `fresh-refuse` — populated target → `--mode=fresh` exits non-zero; marker bytes unchanged.
  - `merge-add` — deleted file is re-added by `--mode=merge`; untouched neighbors confirmed by hash.
  - `local-mod-detect` — `update.sh --dry-run` reports `LOCALLY_MODIFIED` for a target file modified after install.
  - `local-mod-preserve` — `update.sh` with `[K]eep` leaves the local file bytes intact.
  - `backfill-migrate` — legacy shell-format marker migrated to JSON after one `update.sh` run.
- **Portability fix: `sha256sum` vs `shasum -a 256`.** Surfaced by adding macOS to the CI matrix — macOS doesn't ship `sha256sum`. Both `install.sh` and `update.sh` now detect the available command at startup (`detect_sha256`) and route hashing through a shared `$SHA256_CMD`. macOS users can now run the install scripts; previously they would have hit `sha256sum: command not found`.
- **Bug fix in `install.sh`: merge-mode rerun set-e trip.** `write_version_marker` ended with `[ "$marker_existed" = false ] && ADDED_FILES+=("$marker")`. When the marker already existed (re-running `install.sh` in merge mode), the `[` test returned 1, propagated through `&&` to the function's exit code, and tripped `set -e`. The merge install would then roll back the one new file it had just added. Replaced with explicit `if/fi`. Surfaced by the `merge-add` scenario.
- **README CI badge.** Standard GitHub Actions badge directly after the title — current `main` build state is visible at-a-glance for anyone landing on the repo.
- **INSTALLATION.md + ARCHITECTURE.md** got CI sections / rows pointing to the workflow and scenarios fixture.

## [0.8.0] - 2026-05-13 — Per-file hashes enable safe automated updates

- **`.skeleton-version` now stores per-file SHA-256 hashes.** `install.sh` writes a `files` object mapping every installed `.claude/` file's relative path to its content hash at install time. Top-level files (`CLAUDE.md`, etc.) and `--mode=merge` skipped files are not hashed — `update.sh` doesn't touch the former and backfills the latter on first encounter.
- **Schema migration: shell key:value → JSON.** Old markers (versions ≤ 0.7.0) used `version: 0.7.0\nmode: merge\n…` lines; new markers are JSON. Parsing requires `python` (or `python3`) on `PATH`; `jq` is optional / not used. `python` is shipped with most Git Bash for Windows installs.
- **`update.sh` six-way classification.** Each installed file is now bucketed using the recorded / current / template hash triple: `TEMPLATE_UPDATED` (safe to apply), `LOCALLY_MODIFIED` (warn, never auto-update), `UNCHANGED`, `LOCAL_MATCHES_TEMPLATE` (rare), `NEW`, `ORPHAN`. Replaces v1's binary diff that prompted on any difference.
- **`--auto-apply` semantics tightened.** Applies `TEMPLATE_UPDATED` and `NEW` automatically; never `LOCALLY_MODIFIED` or `ORPHAN` — those always require explicit per-file input. Force-disabled during backfill.
- **One-time backfill on pre-0.8.0 markers.** Old shell-format markers trigger a `BACKFILL MODE` warning: prominent banner, forced interactive review, recorded-hash := current-on-disk-hash assumption (can't detect local mods made before migration). Marker is upgraded to JSON after the run.
- **Atomic marker writes.** `install.sh` and `update.sh` both write to `.skeleton-version.tmp.$$` then `mv -f`, preventing partial markers on interrupt.
- **CRLF/LF handling.** Python output is reconfigured to LF on stdout (`sys.stdout.reconfigure(newline="\n")`) and on file open (`open(..., newline="\n")`). Bash side defensively strips CR from dumped marker fields. Discovered during testing — Windows Python 3's text-mode stdout was emitting CRLF, breaking the TSV-to-bash parser.
- **`docs/INSTALLATION.md`** rewritten for update flow: classification table, hash mechanism, backfill notes. v1 limitation section removed.
- **Tests** (against `C:/Users/darre/Dev/test-skeleton-install/`): fresh-install marker validates as JSON with 25 files, all 64-char hex hashes; classification scenarios T2-T5 detect 0 / 1 / 1 / 1 expected counts; T6 backfill prints warning, disables `--auto-apply`, accurately classifies, writes JSON marker. Trainer-View dry-run (existing v0.6.0 marker, untouched) classifies 16 TEMPLATE_UPDATED + 0 LOCALLY_MODIFIED — backfill compatibility confirmed without modifying TV.

## [0.7.0] - 2026-05-13 — Validated on real-world Flutter+Firebase production target

- **Migrated Trainer-View ("Forged In") to claude-skeleton baseline** via `install.sh --mode=merge --claude-only`. 12 new files added (11 baseline + `.skeleton-version`); zero tracked-file modifications to TV's existing 7 agents, 5 skills, 4 hooks, 2 scripts, 13-row ROUTING.md, CLAUDE.md, CLAUDE_MANAGER.md. Non-destructive install validated on real production code. TV's `CLAUDE_MANAGER.md` extended with an "extended frontmatter convention" section (TV-specific `effort` / `memory` / `color` fields acknowledged); `hooks/README.md` extended with an orphan-scripts acknowledgment for TV's two unregistered hooks.
- **`project-tuner-helper` output contract rewritten.** Agent now writes its full report to a file (default `.claude/agent-memory/project-tuner-report-<timestamp>.md`) and returns a brief summary paragraph (path / counts H·M·L / top-3 critical / report-only honor flag). Discovered during TV migration — Agent tool output window truncated a 115k-token inspection between subagent and manager. Returns are now capped at ~300 tokens. Template only; installed copies pick up the fix on next `update.sh` run.
- **`project-tuner-helper` `--report-only` mode formalized.** Adds `PROPOSED:` prefix to every recommendation; disables Edit/Write on project files (still writes the report file itself); self-audits the finalized report and notes any breach in the return paragraph. Documented in agent body and frontmatter description.
- **Subagent registration limitation confirmed (3rd observation).** Phase 3 PolyClaude promotion in Trainer-View, Phase 4c dogfood in this repo, and Phase 4f TV migration all required a Claude Code session restart before newly-installed subagents in numbered folders became dispatchable. Worth documenting as a permanent caveat in `docs/INSTALLATION.md` — deferred to next phase.

## [0.6.0] - 2026-05-13 — Validation milestone

- **Validation on a real target.** `bash scripts/install.sh --mode=fresh --target <throwaway>` exercised against a fresh non-skeleton project (`C:\Users\darre\Dev\test-skeleton-install\`). Verified: 31 files copy correctly (10 agents, 5 skills, 2 scripts, 4 commands, 3 hook files, 1 settings.json, 3 top-level docs, 3 `docs/` templates); `.skeleton-version` written with all six fields populated; top-level and `docs/*` templates render alongside `.claude/` (first run without `--claude-only`).
- **Fresh-mode refusal verified.** Re-running `--mode=fresh` against the populated target exited 1 with the documented error (`--mode=fresh refused: target .claude/ already has content`) and modified no files.
- **Merge-mode non-destruction verified.** Locally prepended a comment to `.claude/agents/01_research/research-helper.md` and added a fake `.claude/agents/01_research/custom-helper.md`. `--mode=merge` skipped all 31 existing files and copied 0; both local mod and custom file preserved verbatim. `.skeleton-version` updated with new `installed_at` and `mode=merge`.
- **Resolved deferred 4b.6 audit findings.** F10 (shipped `template/PLUGINS.md.template` as the canonical plugin log; tightened `CLAUDE_MANAGER.md.template` to drop the `(or equivalent)` hedge); F11 (rewrote `docs/PHILOSOPHY.md` forward reference to non-existent `integration-checker` skill — clarified `integration-installer` handles install mechanics, not plugin-discipline pre-checks); F13 (created `docs/SESSION_LOG.md` at claude-skeleton root, backfilled Phase 4a-4d history — claude-skeleton now dogfoods its own session-log convention); F14 (replaced stub content in `template/docs/{STATUS,SESSION_LOG,ARCHITECTURE}.md.template` with real baseline structure); F17 (added 4 slash-command routing rows — `/commit`, `/audit`, `/deploy`, `/smoke-test` — to `ROUTING.md.template`); F19 (deploy.sh routing row flags `{{DEPLOY_COMMAND}}` placeholder as a prereq for `project-tuner-helper`).
- **`self-audit-helper` severity rubric tightened.** Replaced the three-word severity definitions in the template helper's contract with explicit HIGH / MEDIUM / LOW rubric with examples, plus an "escalate if unsure" guidance line. Addresses the inconsistent grading observed in the Phase 4c dogfood audit.
- **No install bugs surfaced.** Phase A validation ran end-to-end without modifying `install.sh`; the v0.5.0 script handles fresh, fresh-refusal, and merge-non-destruction correctly against a real target.

## [0.5.0] - 2026-05-13 — Install mechanism

- **`scripts/install.sh`** — curl- or local-installable. Three modes (fresh / merge / replace) with the non-destructive rule enforced. `--claude-only` flag for skeleton-on-skeleton installs. `--dry-run` for safe previews. Auto-detects skeleton checkout by walking up from the script; falls back to cloning the public repo. Auto-detects target via `git rev-parse`. Refuses non-git targets, self-installs (without `--claude-only`), and dirty preflight conditions. Writes `.claude/.skeleton-version` after a successful install. On any error, rolls back every file added by the run.
- **`scripts/update.sh`** — diff-driven update flow. Reads `.skeleton-version`, scans each file under `.claude/` against the current template, presents per-category and per-file decisions. Backs up before overwriting; rollback restores on error. v1 cannot distinguish locally-modified from template-updated without per-file hashes — documented as a limitation.
- **New agent**: `integration-installer` (`template/.claude/agents/05_meta/`) — judgment-driven companion to `install.sh`. Inspects target state, decides mode appropriateness, surfaces edge cases, produces a structured install plan with the exact script invocation. Does not modify files itself. Coordinates with `project-tuner-helper` as the second stage.
- **`.gitattributes`** — forces LF endings for `*.sh` and `*.bash` on all platforms. Bash refuses CRLF shebangs.
- **First dogfood install**: `bash scripts/install.sh --mode=merge --claude-only` against this repo. Populated root `.claude/` with 10 agents, 5 skills, 2 scripts, 4 commands, 3 hook files, and `.skeleton-version`. Existing meta-dev `settings.json` preserved by the non-destructive rule. 23 files match `template/.claude/` byte-for-byte (verified). Clears Finding F16 from the 4b.6 meta-system drift audit.
- **`docs/INSTALLATION.md`** — real content (install / update / uninstall / modes / dry-run / troubleshooting) replacing the Phase 4a stub.
- **Limitation**: subagent registration for custom agents under `.claude/agents/` requires a session restart in Claude Code. Skills register live (verified in-session after the dogfood install).

## [0.4.0] - 2026-05-13 — Meta-management layer

- **Four new agents** (`template/.claude/agents/05_meta/`):
  - `system-memory-helper` — system inventory (agents / skills / scripts / commands / hooks / plugins).
  - `agent-slicer` — surgical edits to existing agent files with frontmatter validation.
  - `workflow-suggester` — pattern detection over `SESSION_LOG.md`; pure suggestion.
  - `self-audit-helper` — meta-system drift detection (orphans, dead refs, doc drift, missing routes).
- **Two new skills** (`template/.claude/skills/`):
  - `token-efficiency-monitor` — observational alert when a subtask's cost exceeds 1.5× its expected envelope.
  - `plugin-roster-search` — lightweight capability → handler lookup across `.claude/` and active plugin directories.
- **Routing**: six new rows in `ROUTING.md.template`, grouped by purpose (inventory & search, modification, audit, observation).
- **Architectural milestone**: the meta-system now has recursive ownership — the skeleton can watch, modify, and audit itself. Each agent and skill is context-aware: it walks whatever `.claude/` is active where it runs. Works inside `claude-skeleton` (managing the meta-system's development) and inside any target project after install.

## [0.3.0] - 2026-05-13 — project-tuner-helper

- **New agent**: `project-tuner-helper` (`template/.claude/agents/05_meta/`). Inspects the target project after baseline installation, recommends placeholder fills, helper tightening, and project-specific helpers, awaits user approval, and generates only what was approved. Language-agnostic (Python, JS/TS, Go, Rust, Flutter, Godot, Bash).
- **Routing**: new row in `ROUTING.md.template` mapping "set up new project / install skeleton / tune skeleton to this project" to `project-tuner-helper`.
- **Install flow status**: customization side of the two-agent install flow is now functional. Mechanics side (`integration-installer`) lands in Phase 4c.

## [0.2.0] - 2026-05-13 — Baseline template content

- **Baseline agents** (`template/.claude/agents/`): `research-helper`, `audit-helper`, `monitoring-helper`, `plan-coordinator`. Minimal frontmatter; section-routing documented as a behavioral instruction in each agent body.
- **Baseline skills** (`template/.claude/skills/`): `schema-verify-before-edit`, `post-edit-test-suggest`, `god-file-grep-first`. Each ships with a baseline watched-list and an extension marker for `project-tuner-helper`.
- **Baseline scripts** (`template/.claude/scripts/`): `commit.sh` (5-section verbatim output, path-shape guard) and `deploy.sh` (uncommitted-changes check, `{{DEPLOY_COMMAND}}` placeholder, POST-DEPLOY SMOKE TEST REQUIRED banner).
- **Baseline slash commands** (`template/.claude/commands/`): `/commit`, `/audit`, `/smoke-test` (routing marker — real impl in `browser-tester` plugin), `/deploy`.
- **Baseline hooks** (`template/.claude/hooks/`): `sessionstart-rules.sh` (re-injects `compactPrompt` after compaction) and `precompact-backup.sh` (backs up STATUS / SESSION_LOG / CLAUDE.md). README documents why `PostToolUse` and `SubagentStop` are not shipped (user-level settings shadow project-level blocks).
- **Templated config files**: `CLAUDE.md.template`, `CLAUDE_MANAGER.md.template`, `ROUTING.md.template`, `.claude/settings.json.template` — all with `{{PLACEHOLDER}}` syntax for `project-tuner-helper` to fill at install time.
- **Deferred to 4b.5**: `project-tuner-helper` agent. Designed and built fresh on top of the now-stable baseline.

## [0.1.0] - 2026-05-13 — Pre-alpha foundation

- Project initialized.
- Directory structure established (`.claude/`, `template/`, `scripts/`, `docs/`).
- Initial docs scaffolded (`README`, `PHILOSOPHY`, `ARCHITECTURE`, `INSTALLATION` stub, `CHANGELOG`).
- MIT `LICENSE`, `.gitignore`, `VERSION` (`0.1.0`) in place.
- `.claude/settings.json` configured with plan-mode default and durable rules in `compactPrompt`.
- Two-agent install flow documented in `ARCHITECTURE.md` (`integration-installer` for mechanics, `project-tuner-helper` for customization). No implementation yet.
- No features yet — Phase 4a is foundation only. Features land in subsequent phases:
  - **4b**: populate `template/.claude/` with baseline agents/skills/hooks; build `project-tuner-helper`.
  - **4c**: install/update mechanism + `integration-installer`.
  - **4d-e**: validation, test projects.
