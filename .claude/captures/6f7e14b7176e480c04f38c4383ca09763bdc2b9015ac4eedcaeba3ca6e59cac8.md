---
capture_id: 6f7e14b7176e480c04f38c4383ca09763bdc2b9015ac4eedcaeba3ca6e59cac8
source_pattern_id: 6f7e14b7176e480c04f38c4383ca09763bdc2b9015ac4eedcaeba3ca6e59cac8
source_pattern_type: other
status: shipped
confidence: high
suggested_artifact_type: manual_action
created_at: 2026-07-08T02:10:00Z
shipped_to: .claude/agents/05_meta/self-audit-helper.md
---

# Capture: orphan-detection duplicated across self-audit-helper and artifact-fit-analyzer

Producer: `artifact-fit-analyzer` (Phase 56, non-observation producer — no
observation file backs this capture; id derived per its agent definition).

## Pattern
OVERLAP: `self-audit-helper` (drift category 1, "Orphans") and
`artifact-fit-analyzer` (ORPHAN lane) both cover the job "find `.claude/`
artifacts referenced by no other surface," scanning the same inventory
(agents/skills/scripts/commands/hooks) against the same reference surfaces
(`ROUTING.md`, `CLAUDE_MANAGER.md`, `settings.json`). This is duplication,
not composition: the two agents share no implementation (no common lib;
each runs its own reference scans) and neither's boundary statement names
the other — artifact-fit-analyzer's boundaries delimit only cruft-checker,
manager-optimizer, and code-quality-auditor. The lanes differ only in that
the ORPHAN lane adds an untouched-since-install git condition and outputs
captures rather than a drift report.

## Evidence
- .claude/agents/05_meta/self-audit-helper.md:3 — "Finds orphan files (no other surface references them)"
- .claude/agents/05_meta/self-audit-helper.md:42-43 — "**Orphans** — files in `.claude/` that no other surface references."
- .claude/agents/05_meta/self-audit-helper.md:29-34 — inspects `.claude/agents|skills|scripts|commands|hooks/**` + `ROUTING.md` + `CLAUDE_MANAGER.md` + `settings.json`
- .claude/agents/05_meta/artifact-fit-analyzer.md:57-61 — "**ORPHAN** — an artifact referenced by nothing (no `CLAUDE_MANAGER.md` mention, no `ROUTING.md` route, no settings wiring, no cross-artifact reference) AND untouched since install."
- .claude/agents/05_meta/artifact-fit-analyzer.md:63-70 — boundary section names cruft-checker / manager-optimizer / code-quality-auditor only; `grep -n 'self-audit'` over the file returns no matches
- .claude/agents/05_meta/self-audit-helper.md — `grep -n 'artifact-fit'` returns no matches (neither file delimits against the other)
- CLAUDE_MANAGER.md:290 — roster row: self-audit-helper, "Meta-system drift detection (orphans, dead refs, doc drift, missing routes)."
- CLAUDE_MANAGER.md:454 — boundary line repeats only the three-agent list; self-audit-helper absent

## Suggested response
Manual action: decide which surface owns unreferenced-artifact detection and
codify the boundary. Options, smallest first: (a) add one boundary line to
each agent definition splitting the job (e.g. self-audit-helper keeps dead
registrations / doc drift / missing routes; ORPHAN reporting consolidates in
artifact-fit-analyzer with its git-history condition); (b) narrow
self-audit-helper's category 1 to defer orphan reporting to the ORPHAN lane;
(c) leave both but record the redundancy as accepted in CLAUDE_MANAGER.md's
boundary list. Any option is a user-directed edit — this producer never
edits artifacts.

## Approving / rejecting
Edit `status` in the frontmatter: `approved` (then apply as a user-directed
edit — no X-builder consumes `manual_action`), `rejected` (permanent
do-not-re-suggest marker; keep the file), or `shipped` with `shipped_to:`
after the edit lands. To re-open a rejected finding, delete this file and
re-dispatch artifact-fit-analyzer.
